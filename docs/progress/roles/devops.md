# DevOps（运维/部署工程师）角色日志

## 2026-08-16 — 会话摘要（第八次出场）
- 本次角色：DevOps（运维/部署工程师）
- 动作：Review（v0.1 实现阶段 R1，被指定为 Review 方）
- 涉及文档：`iterations/v0.1-test-report.md`（追加 R1 Review 记录）、`iterations/v0.1.md`（实现阶段门禁行补 DevOps 结论）、`INDEX.md`
- 结论：**需修改（O3 高，同 Architect A14）**。另 O1 放行前置（`l1-gates` 未在实现代码上跑过）、O2 中（Makefile 与 CI 两套口径）。产物落位、凭据纪律、`confirm` 侧证据链、六条路径覆盖均已到位。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - **做了实证而不只读代码**：`make test`（93 passed 属实）、`make lint`、并用 `git archive HEAD` 导出干净检出精确预演 CI 环境（lint 退出 0、93 passed）——三条实现 commit 从未跑过 CI，作为门禁负责人应当预判推上去会不会红，而不是等它红了再说。
  - **撤回过一条误报**：本以为 `submit.py` 全程无 `conn.commit()` 会让 `in_flight` 随未提交事务回滚、N8 失效，查 `db.py:93` 确认 `isolation_level=None`（autocommit）后撤回。但把它写进了 Review 记录——因为 autocommit 是个**隐式前提**，日后有人给 `connect()` 加 `isolation_level`，`submit.py` 会毫无报错地丧失崩溃安全性，值得留一行注释钉住。
  - O1 定性为「放行前置」而非「代码缺陷」：push 归 Owner 决定，与 Developer 产出质量无关；但门禁状态属我的判断范围，必须显式标出，不能因为不是谁的错就不提。
- 关键失误（须记住）：**O3 是我该抓到而漏掉的，由 Architect A14 抓到**。我通读了 `submit.py` 全文、逐条核对 N8/N11/N14/N15，看到 `reconcile(client, conn, *, snapshot_dir)` 的签名就默认该参数被使用了，没验证函数体——实际零次使用，`cli.py` 传了参数直接被丢弃。恢复路径不补齐证据，正是我盯了三轮的那条链上最后一环。教训：**核对「代码在不在」不等于核对「参数用没用」**；这与我自己在设计 R3 写下的「替身按理想情况返回数据会掩盖取数路径错误」是同一类认知陷阱，换成人工阅读同样成立。下次核对契约落地，对每个参数问一句「它在函数体里出现过吗」。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：CN-001（已由 PM 执行落入 PRD 正文）
- 遗留问题/风险：
  - **R2 修订后需重新取得 `l1-gates` 绿灯**——门禁结论绑定 commit、不继承。
  - O2 的 CI 那半边由我承接：把 lint 排除规则对齐为 `docs/research|\.venv`，并改为直接调用 `make lint` / `make test`，从此单一口径。等 Developer 侧把 `PY` 改成可回退写法后一并做。
  - `db.connect()` 的 autocommit 是 `submit.py` 崩溃安全性的隐式前提，建议补注释钉住（非必修）。
  - `.github/workflows/l1-gates.yml` 仍未列入 ADR-0003 受保护路径名单（历次提出，待 Architect 判断）。
- 下一步入口：Developer 出 R2（必修 A14≡O3、A15；建议一并处理 A16-A20 与 O2 的 Makefile 半边）→ push 取得 `l1-gates` 绿灯 → 两方复核 → Owner 验收（含 3 项人工抽检）→ 迭代关闭检查。部署就绪检查阶段预期为「已跳过（本迭代无部署，本地运行）」。
- 收尾状态：未收尾

## 2026-08-16 — 会话摘要（第七次出场，首个非 Review 产出）
- 本次角色：DevOps（运维/部署工程师）
- 动作：产出（建最小 `l1-gates` CI，Owner 指示）
- 涉及文档：`.github/workflows/l1-gates.yml`（新建）、`INDEX.md`（回填下一步入口状态）
- 结论：**已建成，待 push 生效**。Python 3.11 + `compileall` 语法检查 + pytest 三步；三个步骤的行为均已本地实测（语法检查退出 0；测试步骤退出码 0→0 / 5→0 / 1→1）。这条 CI 的来龙去脉正是我在 PRD R1 提的 M1——Owner 先裁「接受降级模式」，设计定稿后再裁「建最小 CI」，闭环到本次执行。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - **不设 `paths-ignore`**：直觉上纯文档 commit 不该跑 CI，但按 `runtime.md` [P0]，「查不到 `l1-gates` 结论」与红灯同等对待、一律不推进——跳过文档 commit 会让这类 commit 永远拿不到绿灯，反而把流程卡死。让每个 commit 都跑一次（骨架期几十秒）是唯一不自伤的选择。
  - **lint 用标准库 `compileall` 而非 ruff/flake8**：设计 §1.2 明确「运行时依赖只有 requests，测试期另加 pytest」，引入 linter 属技术选型、归 Architect。占位 + 注释说明「选定后由 DevOps 替换」，不擅自扩大依赖面。
  - **排除 `docs/research/`**：实测当前 Demo 快照语法是通过的，排除属**预防性**——它是第三方只读快照，其问题不该判本项目红灯；日后若换成 ruff，风格检查会立刻在这 3401 行上炸开。
  - **pytest 退出码 5 视为通过**：骨架期无用例，否则首个 commit 就红灯把流程锁死。其余非零码一律判红，不放宽。
  - **不配置任何平台凭据 secret**：CI 永不接触真实 BRAIN 平台；PRD #6/#8 的真实执行部分按既定口径由 Owner 人工抽验。
  - 撤回了一次重复登记：我本已在异常升级记录写了「裁决变更」一行，核对后发现 PM 会话已于 2026-08-16 登记同一裁决（表内第 2 行），遂删除自己那行——不重复登记、不代改他人已写的裁决记录。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：无
- 执行结果（同日补记）：已 push，**首跑绿灯**（run `31927751637`，10 秒），骨架期行为与本地实测一致——`pytest` 退出码 5 被正确识别为「无用例」。**v0.1 降级模式到此结束**，实现阶段起推进依据回到 [P0] 的 `l1-gates` 绿灯结论。首跑另暴露一条 Node 20 弃用警告：`actions/checkout@v4` / `setup-python@v5` 被强制跑在 Node 24 上；查 GitHub API 后确认两者均已发布至 v7，同日升级消除警告。教训：写 workflow 时 action 版本不能凭记忆写，要查当前版本。
- 遗留问题/风险：
  - **`.github/workflows/l1-gates.yml` 未列入 ADR-0003 受保护路径名单**。ADR 判据是「不可重算」，CI 文件本身可重写、不符合该判据；但删除它等于**静默关闭门禁**，后果是安全性而非数据丢失。建议 Architect 评估是否为此扩一条判据（或明确不纳入的理由）——名单归 Architect，DevOps 只提。
  - 依赖声明形态（`requirements.txt` / `pyproject.toml`）未定，安装步骤三种都容错处理；Developer 定下后由 DevOps 收敛。
  - 骨架期 workflow 未经真实运行验证（本地只验了各步骤行为）。首次 push 后须确认 Actions 页面实际绿灯，若红灯由 DevOps 修。
- 下一步入口：Owner 决定是否 push（本地累计多条未推送 commit，含其他会话产出）→ push 后确认 GitHub Actions 首跑绿灯 → 回填 INDEX 状态 → Developer 启动实现阶段。实现阶段 DevOps 关注点：`.gitignore` 追加 `data/` 的时点、一键验证入口（§8 第 2 条）与本 CI 的关系（建议 CI 直接调用同一入口，避免两套验证口径漂移）。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要（同一会话第六次出场）
- 本次角色：DevOps（运维/部署工程师）
- 动作：Review（v0.1 设计文档 R4 快速复核，Owner 裁决快速通道后的定向修订）
- 涉及文档：`iterations/v0.1-design.md`（追加 R4 Review 记录）、`iterations/v0.1.md`（设计阶段 R4 门禁行补 DevOps 结论）、`INDEX.md`（当前阶段补登）
- 结论：**通过（附条件 N16，一行文本）**。N14/N15 均已落实：§3.1 补 `get_alpha`、§4.6 改逐条直查并删掉增量同步那一步、§8 第 6 条第六项用例一字不差采纳；N15 还补齐了我没写的「全部收敛则退出码 0」。新增 N16 低（§3.1.0 替身方法枚举未同步 `get_alpha`）。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - 严格按「快速复核」范围执行：只核 R3 意见落点，不重审全文。Owner 已裁决快速通道，Review 方扩大范围等于变相推翻裁决。
  - 但**对修法本身做了额外复核**（异常路径、快照批次归属、退出码闭合）——前两轮的教训正是「修法引入新缺陷」，新机制该按新设计审，这不算超范围。
  - N16 定「低」并明示「处置后可直接定稿」：一行文本对齐，不该成为再开一轮 Review 的理由；同时不因为它小就不提——§8 第 6 条第六项的用例正依赖这处枚举。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：CN-001（待 PM 确认，不阻塞设计定稿）
- 遗留问题/风险：
  - 重复 POST 不可逆动作这一漏洞已出现三次形态（N8 → N11 → N14），R4 后取数路径已闭合。实现阶段仍应把「任何能触发 `submit_alpha` 的路径」当作重点审查对象。
  - §8 第 6 条六条「亮绿灯但数据错」路径是降级模式下唯一的兜底，实现阶段须逐条有针对性用例，不能只跑常规验收。
  - 实现阶段 DevOps 关注点：`.gitignore` 追加 `data/` 的时点、一键验证入口的实际覆盖度、`data/logs/` 与 `data/snapshots/` 的落盘行为。
- 下一步入口：等 Developer R4 快速复核 → 两方通过 + N16 处置后 Architect 定稿进实现阶段（降级模式：定稿推进需 Owner 确认 + 留痕）。部署就绪检查阶段预期为「已跳过（本迭代无部署，本地运行）」。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要（同一会话第五次出场）
- 本次角色：DevOps（运维/部署工程师）
- 动作：Review（v0.1 设计文档 R3 复核）
- 涉及文档：`iterations/v0.1-design.md`（追加 R3 Review 记录）、`iterations/v0.1.md`（设计阶段 R3 门禁行补 DevOps 结论）、`INDEX.md`（当前阶段补登）
- 结论：**需修改（N14 高）**。R2 的 N11-N13 三条全部落实（`submit --reconcile` + 退出码 6、快照写盘失败即终止、验证入口标注 #1 口径依据）；但 `--reconcile` 的收敛判据取不到平台真实状态，会自动把「已提交成功」误判为未提交并再次 POST。另提 N15 低（reconcile 遇查询失败的处置未定义）。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - N14 是**独立核出**后才发现与 Developer D21 同源：核 §4.6 时看到收敛第一步依赖 `sync --incremental --stage OS`，回查 §4.2 确认增量语义是 `dateCreated > 水位`，而提交不改 `dateCreated` → 已入库记录永远进不了增量集合。写记录时选择**不复述 D21 的技术论证**（以其为准），只补三点 DevOps 视角增量：失效方向从 fail-safe 退化为 fail-dangerous、属「亮绿灯但数据错」第六条须进 §8 自测清单、`get_alpha` 直查让恢复路径的判据独立于可能已出问题的常规写入链。
  - 结论定「需修改」而非「通过（附条件）」：修法涉及接口契约新增方法（非文本补正），且失效方向危险，不该以附条件的弱形式挂着。虽然这会触发「R3 未通过升级阻塞」规则。
  - 对 R4 与否**只给建议、不自行放行**：如实指出速查 §9-10 已触发「不进 R4、升级 Owner 决策」，同时给出「修入正文→快速确认→定稿」的建议路径并说明两方判断一致——把决策交回 Owner，不擅自降级严重度。
- 关联迭代：v0.1
- 关联非迭代工作：`ad-hoc/2026-08-15-spike-demo-code-firsthand-audit.md`（Demo 一手核验结论落入本轮，DevOps 侧确认 §8 第 5 条新增的「不返回 Response 对象」与 N6 同类——都在数据结构层面封堵凭据泄露）
- 关联 Change Note：CN-001（待 PM 确认，N13 已给出过渡期兜底）
- 遗留问题/风险：
  - N14 未修前不得进实现阶段——实现会话会照 §4.6 写出一个在真实中断现场会触发重复提交的 reconcile，且测试替身查不出来。
  - N15：恢复路径的默认行为应当是「不确定就不动」，未写明时容易被实现成「查不到 = 不是 OS」，即掉进与 N14 相同的危险分支。
  - 同一漏洞（重复 POST 不可逆动作）已在 N8 → N11 → N14 出现三次形态。实现阶段应把「任何触发 `submit_alpha` 的路径」当作重点审查对象。
- 下一步入口：等 Owner 裁决 R3 未通过的流程走向 → Architect 修 N14/D21（+ N15/D22/D23/Q7）→ 两方快速确认后定稿进实现阶段（降级模式：定稿推进需 Owner 确认 + 留痕）。部署就绪检查阶段预期为「已跳过（本迭代无部署，本地运行）」。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要（同一会话第四次出场）
- 本次角色：DevOps（运维/部署工程师）
- 动作：Review（v0.1 设计文档 R2 复核）
- 涉及文档：`iterations/v0.1-design.md`（追加 R2 Review 记录）、`iterations/v0.1.md`（设计阶段 R2 门禁行补 DevOps 结论）、`INDEX.md`（当前阶段补登）
- 结论：**通过**，附条件 N6/N7/N8 解除。R1 的 N5-N10 六条逐条核对落点原文后确认全部落实（未采信修订对照表的声明）。新增 N11 中、N12/N13 低，均不阻塞定稿。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - N11 是我 R1 建议的 `in_flight` 机制带来的新缺口，Review 记录里先说明责任来源再提问题——引入新状态却不给出口是我上一轮没想周全的地方，不掩饰。
  - N11 给两种粒度（自动 `--reconcile` / 最小 `--resolve`）并倾向前者：平台 stage 反查手段设计里已写好，自动化只是把它接上，还能顺带补齐停在意向态的快照。
  - N12 定「低」而非「中」：磁盘满是低概率事件，但后果不对称（无证据的不可逆动作），故在建议里点明「一次 try/except 换掉的是不对称后果」。
  - N6 的落实我额外验证了 Python 语义：`field(repr=False)` 不带 default，password 仍是必填字段，不会触发「非默认参数跟在默认参数后」的定义错误——契约写法成立才算真落实。
  - 仍只追加 Review 章节，未改设计正文与 Review 状态表（正文归 Architect）。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：CN-001（待 PM 确认；DevOps 侧仅在 N13 提示其对 Owner 验收口径的影响，不介入 PM 决策）
- 遗留问题/风险：
  - N11 若延期到实现阶段，须显式记录延期结论——否则首次遇到 `in_flight` 时 Owner 只能手改 SQLite。
  - N13：降级模式下 Owner 验收看的是 PRD 正文，CN-001 未落正文前 #1 存在按旧口径判红的可能。
  - 实现阶段 DevOps 关注点不变：`.gitignore` 追加 `data/` 的时点、一键验证入口的实际覆盖度、`data/logs/` 与 `data/snapshots/` 的落盘行为。
- 下一步入口：等 Developer 设计 R2 复核 → 两方通过后 Architect 定稿进实现阶段（降级模式：定稿推进需 Owner 确认 + 留痕）。部署就绪检查阶段预期为「已跳过（本迭代无部署，本地运行）」。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要（同一会话第三次出场）
- 本次角色：DevOps（运维/部署工程师）
- 动作：Review（v0.1 设计文档 R1，被指定为 Review 方）
- 涉及文档：`iterations/v0.1-design.md`（追加 R1 Review 记录）、`iterations/v0.1.md`（设计阶段门禁行补 DevOps 结论）、`INDEX.md`（当前阶段补登）；参考 `docs/knowledge/decisions/ADR-0001~0004`
- 结论：**通过（附条件 N6/N7/N8）**，无阻塞、无高严重度缺陷。PRD 阶段 N2/N3/N4 全部销账（§1.4 `data/` 统一产物目录 + 首个产物 commit 前追加 `.gitignore`；§2.3 断点四问全答；§8 一键验证入口）。新增 N5·N9 文本对齐、N6·N7·N8 中、N10 低。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - 初判「快照落在可删的 `data/` 下」是高severity缺陷，核实 ADR-0003 后**下调为 N5 中**——ADR 已把 `data/snapshots/**` 列为受保护路径且论证充分，真实问题只是设计 §7.4 摘要漏了这个例外。教训：设计正文与 ADR 冲突时先读 ADR，别按正文下结论。
  - N6（`Credentials` dataclass 默认 repr 泄露 password）定「中」而非「低」：白名单式日志管不住 traceback，凭据纪律是 project-context 硬约束，且修复成本为零（`field(repr=False)`）。
  - N7 给两个可选方案（日志落 `data/logs/` 或只输出 stderr）而不指定其一：日志形态属 Architect 决策；DevOps 的硬要求只是「必须定下来」——否则 #9 的自动验收没有确定的断言对象。
  - N8 倾向方案 (a) `in_flight` 意向标记而非 (b) 反查平台：一次额外写库换掉不可逆动作的状态歧义，比事后反查更省事且更早止损。
  - 仍只追加 Review 章节，未改设计正文与 Review 状态表（正文归 Architect）。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：无
- 遗留问题/风险：
  - N6/N7/N8 若被判定延期到实现阶段，须显式记录延期结论——尤其 N7，它直接决定 #9 自动验收的断言对象。
  - 设计 §7.1 R-1（预判最坏 14~28 小时）已由 Architect 提请 Owner 知悉，DevOps 无异议：设计侧压缩手段已用尽，下限由平台速度决定。
  - 降级模式生效中，实现阶段的一键验证入口是唯一门禁，其覆盖度须对齐 PRD #1–#10 全部自动部分（N9）。
- 下一步入口：等 Developer 设计 R1 反馈 → 两方通过后 Architect 定稿进实现阶段（降级模式：定稿推进需 Owner 确认 + 留痕）。实现阶段 DevOps 关注点：`.gitignore` 追加 `data/` 的时点、一键验证入口的实际覆盖度、快照与日志的落盘位置。部署就绪检查阶段预期为「已跳过（本迭代无部署，本地运行）」。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要（承前，同一会话第二次出场）
- 本次角色：DevOps（运维/部署工程师）
- 动作：Review（v0.1 PRD R2 复核，Owner 指定）
- 涉及文档：`iterations/v0.1-prd.md`（追加 R2 Review 记录）、`iterations/v0.1.md`（R2 门禁行补 DevOps 结论）、`INDEX.md`（当前阶段补登）
- 结论：**通过**，附条件解除。R1 六条全部销账——H1/M2/M3/M4/L1 由 PM 在 R2 落到验收 #6/#8/#9/#10、§3.1 字段分层与 §5 设计约束；M1 由 Owner 裁决（接受 v0.1 全程降级模式）核销。新增 N1 中（PRD 风险 6 文本未同步 Owner 裁决，属文本过期）、N2-N4 低（运行产物 gitignore 未定 / 断点状态归属未定 / 降级模式缺一键验证入口），均不阻塞定稿。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - N1 定「中」而非「低」：PRD 即将定稿，正文写「待 Owner 拍板」与异常升级表的既成裁决冲突，会误导后续冷启动会话对推进依据的判断——是真源不一致，不是措辞问题。
  - N4 写成「对既定裁决的补强」而非重提建 CI：Owner 已裁决不建 CI，不再 re-litigate；但降级模式下证据质量依赖可复制执行的验证入口，这与建不建 CI 是两件事。
  - N2/N3 建议「结转设计阶段」而非回 PRD 修改：两者都是实现载体问题，写进 PRD 属过度约束。
  - 仍只追加 Review 章节，未改 PRD 正文；Review 状态表 DevOps 行仍为 R1 口径，已在记录中请 PM 定稿时更新。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：无
- 遗留问题/风险：
  - N2（运行产物必须入 `.gitignore`）在实现阶段首个 commit 前必须落实，否则 1 万+ 条库存库有被误提交进仓库的实际风险。
  - v0.1 全程降级模式已生效，每阶段推进需 Owner 确认 + 阶段执行记录写降级留痕。
  - 平台认证 session 生命周期与限流阈值仍无实测（风险 5，设计阶段必答），直接影响 #6 分批节流与 #10 断点续跑的参数。
- 下一步入口：等 Architect R2 复核 → 三方通过后 PM 定稿（降级模式：定稿推进需 Owner 确认 + 留痕）。设计阶段若涉及运行产物目录、断点状态、凭据加载、节流策略，DevOps 可被指定为 Review 方。部署就绪检查阶段预期为「已跳过（本迭代无部署，本地运行）」。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要
- 本次角色：DevOps（运维/部署工程师）
- 动作：Review（v0.1 PRD R1）
- 涉及文档：`iterations/v0.1-prd.md`（追加 Review 记录）、`iterations/v0.1.md`（更新 PRD 阶段门禁 Review 结果）
- 结论：**通过（附条件）**——无阻塞级缺陷。提 1 高（H1 提交动作无回滚路径定义）、4 中（M1 无 CI 致全程降级模式 / M2 验收 #6 自动验证会真实调用平台 / M3 长任务韧性未覆盖 / M4 有状态资产无备份恢复要求）、1 低（L1 凭据路径硬编码）。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - H1 选「要求 dry-run + 提交清单快照」而非「要求实现撤回功能」——平台是否支持撤回属未知事实，须设计阶段确认；在此之前只能要求可事后追溯，不能凭空要求回滚能力。
  - M1 选「交 Owner 二选一（建最小 CI / 明示接受降级）」而非「直接要求本迭代建 CI」——建 CI 属新增工作量，范围决策权在 Owner，DevOps 只负责把「默认漂移成降级且无留痕」这个流程缺陷摆上台面。
  - M4 选「原始数据不可变 + 派生结果可重算」的设计约束，而非引入版本化存储——后者对 v0.1 过重；满足该约束后备份退化为复制单文件。
  - 全部结论只走「追加 Review 章节」，未改 PRD 正文——正文（含 Review 计划 / Review 状态表）写权限归 PM（runtime.md [P0]）。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：无
- 遗留问题/风险：
  - `l1-gates` 门禁不存在（仓库无 `.github/`），v0.1 若不建 CI 将全程降级模式，需 Owner 拍板。
  - BRAIN 平台认证 session 生命周期与限流阈值至今无实测记录，长任务（1 万+ 条同步）韧性存疑。
  - 本 PRD 原以「v0.1 不部署」为由未指定 DevOps，Owner 追加后正文 Review 计划/状态表仍缺 DevOps 行，待 PM 定稿时补入。
- 下一步入口：等 PM 处理 H1/M2 与 Owner 对 M1 的裁决；设计阶段若涉及存储选型 / 凭据加载 / 节流策略，DevOps 可被指定为 Review 方。部署就绪检查阶段预期为「已跳过（本迭代无部署，本地运行）」。
- 收尾状态：未收尾
