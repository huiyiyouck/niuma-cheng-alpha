# Architect 角色日志

## 2026-08-15 — 会话摘要
- 本次角色：Architect（架构师）
- 动作：Review
- 涉及文档：`iterations/v0.1-prd.md`（R1 Review 记录追加）、`iterations/v0.1.md`（门禁表 Review 结果）、`docs/progress/INDEX.md`（项目级状态）
- 结论：**不通过**。1 条阻塞（A1）+ 4 条高（A2-A5）+ 3 条中（A6-A8），PRD 需进 R2。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - A1 未直接判「不可行」而是给出两条出路（前置 spike / 降级为「预判」口径）——因为「平台无此能力」尚未被证伪，只被证明「未验证」；范围口径属产品决策，Architect 不代 PM 决定。
  - A4 建议用 `dateCreated` 时间窗切片求和作为独立对账路径：既让验收可证伪，也顺带是绕过 offset 疑似封顶的技术方案（一石二鸟，已由 demo 源码确认参数存在）。
  - 三条已知的架构约束（提交结果回写口径、库存库存储选型、可注入平台客户端替身）**不**列为 PRD 缺陷，留设计阶段自己承接——避免把设计决策倒灌回 PRD。
- 关联迭代：v0.1（PRD 阶段 R1）
- 关联非迭代工作：无（A1 若选 spike 方案，将新建 `ad-hoc/YYYY-MM-DD-spike-pending-checks.md`）
- 关联 Change Note：无
- 遗留问题/风险：
  - A1 未决前不能进设计阶段（已登记为 INDEX 阻塞项）。
  - demo 的相关性阈值硬编码 0.7 与 project-context「达标线动态化」硬约束冲突，实现阶段须防止照搬 demo 引入该硬编码。
- 下一步入口：PM 会话出 PRD R2；R2 送审后 Architect 另开会话复审（本会话是 Review 方，非产出方，可复审同一文档）。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要（R2 复核）
- 本次角色：Architect（架构师）
- 动作：Review（PRD R2 复核）
- 涉及文档：`iterations/v0.1-prd.md`（R2 Architect Review 记录追加）、`iterations/v0.1.md`（R2 门禁行 Review 结果）、`docs/progress/INDEX.md`（三方齐 → 转 PM 定稿）
- 结论：**通过（附条件 A9/A10）**。R1 的 A1-A8 全部核销、阻塞解除；新增 A9/A10（中）、A11-A13（低），均不阻塞定稿。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - 判「通过（附条件）」而非「不通过」：R2 无阻塞、无高严重度，A9/A10 的修法都是加字段 / 加一句口径，走 R3 完整轮次不划算；与 R1 时因存在阻塞项而判不通过的标准一致，也与 DevOps R1「通过（附条件）」的用法一致。
  - A9（预判回环）本可归入 D5 的保护态补丁，但我判定 D5 的清单**覆盖不到**这条路径（记录停在「改造候选」而非三个保护态），故独立列条——这是成本错误而非逻辑错误，不列出来就会在实现阶段以「重复消耗配额」的形式暴露。
  - A10 选「分流理由加阈值 + 规则版本两个字段」，未选版本化存储：后者对 v0.1 过重，而两个字段即可让任一历史结论可解释、可重现。
  - 阈值 0.3 的跨 check 量纲问题**不**列为 PRD 缺陷，只列设计阶段承接项——淘汰候选不触发破坏性动作，误判成本低，v0.1 用全局默认值可接受。
- 关联迭代：v0.1（PRD 阶段 R2）
- 关联非迭代工作：无
- 关联 Change Note：无（风险 2 保留了「探明提交前检查触发端点后经 Change Note 升级 #6」的路径，设计阶段查证）
- 遗留问题/风险：
  - A9/A10 若 PM 选延期，由我在设计阶段的数据模型中落定（预判结果字段独立性、分流理由的阈值与规则版本留痕）。
  - 设计阶段承接项已在 Review 记录 §四 列全：时间窗切片方案、指纹计算范围、阈值配置粒度、提交成功判定口径、存储选型、可注入客户端层。
- 下一步入口：PM 会话定稿 PRD（降级模式：需 Owner 确认 + 阶段执行记录留痕）→ 进设计阶段时切 Architect 产出 `v0.1-design.md`（届时我是产出方，Review 方另指定）。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要（设计阶段 R1 产出）
- 本次角色：Architect（架构师）
- 动作：产出
- 涉及文档：`iterations/v0.1-design.md`（新建，R1 送审）、`knowledge/decisions/ADR-0001~0004`（新建）、`knowledge/INDEX.md`（ADR 登记）、`iterations/v0.1.md`（设计阶段门禁 R1 行）、`docs/progress/INDEX.md`
- 结论：设计 R1 送审，指定 Developer + DevOps 两方 Review。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - **窗口级对账**（ADR-0002）：PRD #1 主口径若按字面实现（本地条数 vs 切片求和），因切片是唯一写入路径而两数恒等——会退化回 R1-A4 的自证循环。把断言下沉到「每个叶子窗口实际拉取数 == 平台报告 count」才可证伪。这是本次设计里最关键的一处「照字面实现会错」。
  - **窗口边界重叠 1 秒**：平台 `dateCreated>` / `dateCreated<` 两端都是开区间，首尾相接会让边界记录被两侧同时排除而**静默丢失**；重叠 + 主键去重，与 PRD「去重条数」口径天然契合。
  - **预判两阶段短路**（ADR-0004）：依据 Demo skill 记载的「self-corr > 0.7 则 prod-corr 必然 > 0.7」，self 超阈即判死不查 prod，最坏调用次数减半；误判方向保守（不会造成误提交），且 `short_circuited` 留痕可批量复查。
  - **只存 raw_json 不逐字段建列**（ADR-0001）：拆列会让同一数据两处存储，与「平台原始不可变、单一真源」冲突；1 万条 JSON 解析在手动命令下是秒级，不值得用一致性风险换查询便利。
  - **原始/派生分两张表**：让「同步不触碰派生」由 schema 物理保证，而不是靠实现者的代码纪律——PRD §3.1 是硬约束，值得用结构兜。
  - **预判断点不建表**：「已有结论就跳过」与「断点续跑」是同一件事，复用 `prediction_result` 非空即可，两个状态载体必然漂移。
  - **不选本地 PnL 快算**：PRD #6 定稿口径写明走 self/prod 接口；本地快算引入新数据面，且本地相关性算法需自行验证与平台口径一致。已留 `CorrelationResult` 边界，v0.2 换实现不动上层。
  - **受保护路径判据定为「不可重算」**（ADR-0003）：`data/snapshots/` 纳入（提交证据，不可由库重算）、`data/inventory.db` 排除（删了重跑即可）。保护可重算的东西会把门禁变成橡皮图章。
- 关联迭代：v0.1（设计阶段 R1）
- 关联非迭代工作：无
- 关联 Change Note：无
- 遗留问题/风险：
  - **R-1（提请 Owner，非门禁）**：相关性接口最坏 100 秒/条，预判耗时随候选规模线性放大；短路减半后下限仍由平台速度决定。若 Owner 据此收窄范围，走 Change Note。
  - PRD 风险 2/3（提交前检查触发端点、平台撤回能力）：在**文档证据范围内**已查证——Demo 全代码 + pip 包比对 + API 实测事实三者一致指向不存在。真正证伪需对平台发起只读探测（需真实凭据），我未在设计会话中自行执行，已在 §7.3 写明并建议 Owner 授权后再做。
- 下一步入口：Developer / DevOps 另开会话冷启动 Review 设计 R1 → 两方通过后回 Architect 会话定稿（降级模式需 Owner 确认 + 留痕）→ 进实现阶段（Developer 产出方，届时我转 Review 方）。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要（设计 R2 修订）
- 本次角色：Architect（架构师）
- 动作：修改（处置设计 R1 两方 Review 意见）
- 涉及文档：`iterations/v0.1-design.md`（R2 修订 + 修订对照）、`knowledge/decisions/ADR-0001`（R2 修订节）、`ADR-0002`（R2 修订节）、`change-notes/CN-001`（新建）、`iterations/v0.1.md`（R2 门禁行 + Change Notes 表）、`docs/progress/INDEX.md`
- 结论：15 项意见（D9-D15 + Q5/Q6、N5-N10）逐条验证后**全部采纳，无驳回项**；设计转 R2 待复核。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - **D9 是我自己推理不彻底的产物**：我对相邻窗口的开区间边界做了重叠处理，却没把同一条推理应用到根窗口自身——局部正确 + 最外层遗漏。更该记住的是它的失败模式：`reported_count` 与 `fetched_count` 同源于一个开区间查询，丢的记录两边都不在，逐窗口对账必然全绿。**我设计的对账查不出我设计的 bug。** 已把这条教训写进 ADR-0002 而非只改代码步骤。
  - **D10 的修法选「只创建不覆盖」而非放宽字段分层**：要保护的是「已有派生数据不被同步覆盖」，不是「同步不得创建行」。两者混同会让 PRD 的初始态要求无法落地。副产物是 `funnel_status` 可声明 NOT NULL，从根上消除 `NULL NOT IN` 陷阱——比在 WHERE 里打补丁干净。
  - **D14 暴露了我 A9 修法的残留**：我在 PRD R2 复核时提 A9（预判回环重复烧配额），PM 采纳的修法保住了预判**结论**，但没保住结论在**状态层**的后果——记录会被重算回「可提交候选」却因有结论被永久跳过，成为死状态。提意见时只想到配额，没想到状态。
  - **D12 选走 Change Note 而非在设计里单方面声明**：PRD 正文归 PM，Architect 在设计文档里宣布「以设计为准」等于单方面覆盖 PM 定稿的验收标准。同时在 §4.1 写明未确认前的唯一依据，避免实现会话卡住。
  - **N8/Q6 合并处置为「先落意向快照 + in_flight 标记」**：快照是不可逆动作的唯一证据，等全部提交完成才落盘，恰好在最需要证据的时刻（提交过程中死亡）一份都没有。两次写同一文件不损害证据性，且与库中 `in_flight` 互为印证。
  - **四条「亮绿灯但数据错」的路径单列 §8 第 6 条**：D9/D10/D11/D14 的共同点是失败时不报错、不判红，降级模式无 CI 兜底，只能靠针对性用例覆盖——把它们从修订记录提升为实现阶段的显式自测清单。
- 关联迭代：v0.1（设计阶段 R2）
- 关联非迭代工作：无
- 关联 Change Note：`CN-001-验收1主口径改为窗口级对账.md`（待 PM 确认）
- 遗留问题/风险：
  - CN-001 待 PM 确认；若 PM 驳回，需回设计给出满足 PRD 原口径且可证伪的方案——目前未找到（原口径的恒等性来自「切片是唯一写入路径」这一结构事实）。
  - PRD 风险 2/3 的平台只读探测仍待 Owner 授权，本轮未推进。
  - R-1（预判耗时量级）维持提请知悉状态，本轮修订未改变其量级。
- 下一步入口：Developer / DevOps 另开会话冷启动复核设计 R2 → 两方通过后回 Architect 定稿（降级模式需 Owner 确认 + 留痕）→ 进实现阶段。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要（Demo 全量代码一手核验 · Tech Spike）
- 本次角色：Architect（架构师）
- 动作：技术预研（非迭代）
- 涉及文档：`ad-hoc/2026-08-15-spike-demo-code-firsthand-audit.md`（新建）、`docs/progress/INDEX.md`
- 触发：Owner 质询设计的信息来源——我此前只直接读过 `platform_functions.py` 的 185 行（7%），其余 9 个文件 0 行，部署链路完全未研究；设计中的认证自愈、重试参数、ADR-0004 短路依据全部转述自 PM 的 `demo-code-deep-dive.md`。Owner 指示先调研再出 R3。
- 结论：通读全部 10 个文件 3401 行。deep-dive 总体可靠，但查出**一处结论性错误**；另有一处数字订正、一处优化机会、两条新增反面案例。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - **接受质询而非辩解**：我此前的引用标注基本诚实（该标 deep-dive 的标了 deep-dive），但设计 §7.3 写「已查证的证据面：`platform_functions.py` 全量 2600 行」时主语含糊，读起来像我通读过——这处措辞必须在 R3 订正。二手结论用于架构决策时，「谁查证的」和「结论是什么」同样重要。
  - **反修正 deep-dive §8 第 1 条**：它称「MCP 层无批量仿真」，实际 `create_multi_simulation`（2293-2506 行）就在 39 个工具清单内。我不改 PM 的产出正文，只在 spike 记录里写明并列为 PM 待处置。
  - **对「待 PM 处置」的清单做了二次核实**：初版我写「立项 README 表述需回退」，属未经核实的推断——实际查证后发现该表述在 `docs/research/README.md:14`（非项目根 README），且**从未被改动、至今正确**，deep-dive 只是声称修正而未实际改源文件。已订正 spike 记录。教训与本次调研同源：转述他人结论时，「他说已经改了」和「文件真被改了」是两件事，必须分开验证。
  - **ADR-0004 的短路依据仍无法验证**：`brain-calculate-alpha-selfcorrQuick` 属 SOP 层、确认不在快照内。通读没能改变这一点——诚实的做法是在 ADR 风险节补一句「源文件不在本仓库快照内」，而不是让「已通读 Demo」这个事实给它背书。
- 关联迭代：v0.1（设计阶段 R2 → R3 之间）
- 关联非迭代工作：`ad-hoc/2026-08-15-spike-demo-code-firsthand-audit.md`
- 关联 Change Note：无
- 遗留问题/风险：
  - 待落入设计 R3：R-1 耗时数字订正（80s 非 100s）、§4.1 分页 `limit` 可提到 500、§8 补第四条「不返回 Response 对象」、§7.3 措辞订正、ADR-0004 风险节补注。
  - PM 待处置：deep-dive §8 第 1 条订正、立项 README 表述回退。
  - B3（轮询 `"0" == 0` 类型 bug）拟入 `docs/knowledge/engineering/`，v0.2 做仿真调度时必须避免照抄。
- 下一步入口：Architect 出设计 R3——处置 Developer D16-D20 + DevOps N11-N13，并把本次调研结论一并落入正文。
- 收尾状态：已收尾

## 2026-08-15 — 会话摘要（设计 R3 修订）
- 本次角色：Architect（架构师）
- 动作：修改（处置设计 R2 两方 Review 意见 + 落入 Demo 一手核验结论）
- 涉及文档：`iterations/v0.1-design.md`（R3 修订 + R2→R3 修订对照）、`knowledge/decisions/ADR-0004`（证据强度说明）、`iterations/v0.1.md`（R3 门禁行）、`docs/progress/INDEX.md`
- 结论：两方 8 项意见（D16-D20 / N11-N13，D19≡N11）逐条验证后**全部采纳，无驳回**；设计转 R3 待复核。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - **D16 不按建议补 `ifnull()`，而是把判断移出 SQL**。Developer 给的修法（`ifnull(prediction_result,'') = 'fail'`）能修好这一处，但同类问题还会再长出来——他自己在「确认到位」里就写了「凡是靠 WHERE 里的条件表达式去处理 NULL，就还会再踩一次」。所以 R3 让适用域只排保护态（无 NULL 参与），`fail` 判断放进 Python 写回逻辑（`None == 'fail'` 为 False，二值逻辑）。**副产物：D17 的正文/SQL 矛盾自动消失**——记录参与重算，理由照常刷新，只锁状态与来源。一处改动消两条意见。已用 SQLite 实机复现缺陷 + 验证修法，不靠推理。
  - **D16 是我第三次在同一类问题上栽跟头**：D9（根窗口边界）、D10（`NULL NOT IN`）、D16（`= 'fail'` 对 NULL）——全是「边界/空值在 SQL 三值逻辑里静默吞掉记录」，且全部以绿灯形态通过验收。已把通用教训写进 §4.3 正文而非只写修订记录：能在 schema 上消除 NULL 就消除，不能消除就把判断挪出 SQL。
  - **D20 选「让断言恒成立」而非「加适用前提」**：补「已回测」计入右侧后，刚同步未分流时断言同样成立。带前提的断言容易在前提不满足时被当成噪音跳过，恒成立的断言才有守门价值。
  - **D19+N11 选自动收敛（`--reconcile`）而非人工裁定命令**：判据来自平台自身 `stage`，不是本地推测；两个方向都不产生新的不可逆动作。Owner 触发这条路径时正处在一次不可逆动作的中断现场，不该让他手写 SQL 或做判断。
  - **N12「证据写不下就不执行不可逆动作」**：一次 try/except 的成本，换掉「在没有任何证据的情况下执行唯一不可逆动作」的可能——后果完全不对称。
  - **一手核验结论落入正文时，把「谁查证的」也写进去**：§7.3 改成拆表标明一手/二手，并显式写明订正原因（R2 版主语含糊，读起来像我通读过）。ADR-0004 补「证据强度说明」明说短路依据的源文件不在快照内、不因通读获得背书——**通读改变的是我读过什么，不是那条结论的证据强度**。
- 关联迭代：v0.1（设计阶段 R3）
- 关联非迭代工作：`ad-hoc/2026-08-15-spike-demo-code-firsthand-audit.md`
- 关联 Change Note：`CN-001`（待 PM 确认；N13 已给出过渡期兜底）
- 遗留问题/风险：
  - CN-001 待 PM 确认；PRD 风险 2/3 的平台只读探测待 Owner 授权；R-1 维持提请知悉（数字已订正为 80s/条，量级不变）。
  - 归 PM：`demo-code-deep-dive.md` §8 第 1 条订正（不阻塞）。
  - B3（Demo 轮询 `"0" == 0` 类型 bug）仍待入 `docs/knowledge/engineering/`，v0.2 做仿真调度前必须落。
- 下一步入口：Developer / DevOps 另开会话冷启动复核设计 R3 → 两方通过后回 Architect 定稿（降级模式需 Owner 确认 + 留痕）→ 进实现阶段。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要（设计 R4 定向修订）
- 本次角色：Architect（架构师）
- 动作：修改（处置设计 R3 两方意见）
- 涉及文档：`iterations/v0.1-design.md`（R4 修订 + R3→R4 修订对照）、`iterations/v0.1.md`（R4 门禁行 + **异常升级记录第 2 行**）、`docs/progress/INDEX.md`
- 流程前提：R3 两方均「需修改」，按速查 §9-10 触发「升级阻塞交用户决策，不进 R4」。**Owner 裁决走快速通道**（修入正文 → 两方快速确认 → 定稿），已在异常升级记录留痕。我未自行进 R4——这是规则明确要求 Owner 决策的点。
- 结论：R3 两方 6 项意见（D21-D23 + Q7 / N14-N15，N14≡D21）逐条验证后**全部采纳，无驳回**；设计转 R4 待快速复核。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - **D21 是我 R3 亲手引入的,而且比它要修的问题更危险**。我在 §4.2 写下「已入库 Alpha 的平台字段刷新不属增量职责」，又在 §4.6 用 `sync --incremental` 去刷新一条已入库记录的 `stage`——同一份文档、相隔几节、自相矛盾。后果不是「修不好」，而是收敛读到旧值 `IS` → 退回「待确认」→ 下一轮**再次 POST 同一个不可逆动作**，正是这条机制存在的理由。N14 的定性比我自己更准：N8 原始缺陷会**停下来要人工核对**（fail-safe），我的修法在**无人参与时自动误判并重提**（fail-dangerous）。
  - **修法选 `get_alpha` 直查而非 `sync --full --stage OS`**：后者虽可行（OS 仅 88 条），但仍走本地库与水位逻辑。N14 ③ 点出的原则更重要——**恢复路径的判据必须绕开常规写入链**，它不该依赖那条可能已经出问题的链路。这条已写进 §4.6 正文，不只留在修订记录。
  - **连续两轮，我的修法本身引入高严重度缺陷**（R2 的 D14→D16、R3 的 D19→D21）。共同点不是粗心，而是**补救机制时只把它当成「填一个洞」，没有把新机制当作新设计去审**。已把两条由此得出的原则写进正文：判据独立性（§4.6）、替身构造不得掩盖取数路径错误（§8 第 6 条）。
  - **N15 选「不确定就不动」**：查询失败保持 `in_flight`、不猜测、不收敛。恢复路径的默认行为必须是保守的——这正是 D21 失效方向反过来的那一面。
  - **D23 守卫收窄到 `status_source`**：Developer 指出「因 checks FAIL 的改造候选」重新预判通过后会进提交清单，让一条 checks 未达标的 Alpha 排队等提交。平台大概率会拒，但**不该由本地放行**——边界该在本地就收住。
- 关联迭代：v0.1（设计阶段 R4）
- 关联非迭代工作：无
- 关联 Change Note：`CN-001`（待 PM 确认）
- 遗留问题/风险：
  - CN-001 待 PM 确认；PRD 风险 2/3 的平台只读探测待 Owner 授权；归 PM 的 deep-dive §8 订正（不阻塞）。
  - B3（Demo 轮询 `"0" == 0` 类型 bug）仍待入 `docs/knowledge/engineering/`，v0.2 做仿真调度前必须落。
  - **本轮新增的自我提醒**：下次再为某个缺陷补救机制时，先把补救方案当成一份新设计过一遍 Review 视角——尤其问「它的取数/判据从哪来」「它的失效方向是保守还是危险」。
- 下一步入口：Developer / DevOps 另开会话快速复核 R4（只核 R3 意见落点，不重审全文）→ 两方通过后回 Architect 定稿（降级模式需 Owner 确认 + 留痕）→ 进实现阶段。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要（R4 定稿前处置，等 Owner 确认）
- 本次角色：Architect（架构师）
- 动作：修改（处置 R4 两条低严重度附注）
- 涉及文档：`iterations/v0.1-design.md`（§3.1.0、§4.6、§8 第 4 条 + 「R4 定稿前处置记录」）、`docs/progress/INDEX.md`
- 结论：R4 两方**均通过**；N16、D24 两条低已处置。**设计具备定稿条件，但未自行定稿**——定稿是阶段推进，按 `runtime.md` [P0] 与全程降级模式须 Owner 确认。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - **D24 选「顺手 UPSERT」而非「留实现阶段」**：Developer 给了两个选项。详情已在 `get_alpha` 的返回里、零额外请求，让两张表当场自洽比留一个已知不一致更好。同时在正文写明这**不违反字段分层**——写的是平台原始字段、用的是刚取回的权威数据，与 §4.2 同性质，免得实现方按「同步只创建不覆盖」的印象犹豫。
  - **N16 不只补一个方法名，而是标注该枚举是穷举**：根因是「新增第五个方法后没同步这份清单」。只补 `get_alpha` 会让下次新增方法时重演，注明「穷举、新增必须同步」才是修根。
  - **刻意不做超出意见范围的改动**：前两轮的教训正是「补救引入新缺陷」（D14→D16、D19→D21）。本轮两条都是文本与一行逻辑，我没有借机顺手优化任何别的地方。
  - **不自行把门禁置为「已定稿」**：两方都放行了，但阶段推进的授权在 Owner。R3 那次我也没自行进 R4——同一条规则。
- 关联迭代：v0.1（设计阶段 R4 定稿前）
- 关联非迭代工作：无
- 关联 Change Note：`CN-001`（待 PM 确认，不阻塞设计定稿）
- 遗留问题/风险：
  - 待 Owner 确认定稿 + 在「阶段执行记录」写设计阶段降级留痕。
  - CN-001 待 PM 确认；PRD 风险 2/3 的平台只读探测待 Owner 授权；归 PM 的 deep-dive §8 订正（均不阻塞）。
  - **B3（Demo 轮询 `"0" == 0` 类型 bug）仍未入 `docs/knowledge/engineering/`** ——v0.2 做仿真调度前必须落，已连续三轮记在遗留项里，建议实现阶段或收尾时清掉。
  - Developer R4 建议：实现阶段自测报告按 §8 第 6 条「亮绿灯但数据错」六条清单逐条留证据。这条建议值得在实现阶段开工时转达。
  - **已登记跨任务待办（P1）：实现阶段开工前裁决是否建最小 `l1-gates`**（Owner 口述要求登记，2026-08-15）。登记理由是**时机窗口唯一**——此前无代码无测试、CI 空转；此后 N4 的 `make test` 已是 CI 要跑的内容，增量成本很小。维持降级模式同样有效，但要显式再裁一次，避免 DevOps M1 警告的「默认漂移成降级而无留痕」。
- 下一步入口：**Owner 确认定稿** → Architect 置 `v0.1.md` 设计阶段门禁为「已定稿」+ 写「阶段执行记录」降级留痕 → 进实现阶段（Developer 产出代码，届时我转 Review 方）。
- 收尾状态：已收尾

## 2026-08-15 — 会话摘要（设计阶段定稿）
- 本次角色：Architect（架构师）
- 动作：产出（定稿归档）
- 涉及文档：`iterations/v0.1-design.md`（整体状态→已定稿）、`iterations/v0.1.md`（设计阶段增「R4 定稿」行 + **阶段执行记录补设计阶段降级留痕** + 概览转「实现」）、`knowledge/decisions/ADR-0001~0004`（提议→已采纳）、`knowledge/INDEX.md`、`docs/progress/INDEX.md`
- 结论：**Owner 于 2026-08-15 确认定稿**。设计阶段关闭，进入实现阶段。R1–R4 四轮 Review，两方全通过，累计 31 条编号意见 / 29 个独立问题全部采纳、无一驳回。
- 关键取舍（选了什么 / 备选什么 / 为什么）：
  - **定稿时顺手把四份 ADR 从「提议」转「已采纳」**：ADR 自身写明了转换条件（「待 v0.1 设计文档 Review 通过后转」），条件已满足。不转会让知识库长期停在「提议」状态，后续会话读到时无法判断这些决策是否生效。
  - **顺手订正 ADR-0004 背景段的旧数字**（100 秒 → 80 秒）：这是 D22 的同型问题在 ADR 里的残留——R3 只改了风险节，背景段漏了。同一份文档两个数字，读者取哪个都可能。
  - **INDEX 当前状态区做了精简**：删掉「Owner 已裁决（流程）」那条（快速通道裁决已在迭代记录异常升级表留痕，INDEX 只保留「现在卡在哪、下一步做什么」）。同时把 CI 待办标为**已到期**——设计定稿正是那个时机窗口。
  - **下一步入口写成了「开工要点」而非只留路径**：把 §8 第 1/3/6 条的关键动作直接抄进 INDEX，让 Developer 冷启动会话不必先通读设计才知道从哪下手；其中第 6 条那份「亮绿灯但数据错」六条清单是本项目最有价值的实现期检查表，Developer 在 R4 建议自测报告按其逐条留证据，我把这条建议一并转达了。
- 关联迭代：v0.1（设计阶段定稿）
- 关联非迭代工作：`ad-hoc/2026-08-15-spike-demo-code-firsthand-audit.md`（已收尾）
- 关联 Change Note：`CN-001`（待 PM 确认，不阻塞；实现阶段以设计 §4.1 步骤 4 为准）
- 遗留问题/风险：
  - **待 Owner 裁决（已到期）**：是否建最小 `l1-gates`。已登记跨任务待办 P1 并在 INDEX 状态区标注到期。
  - CN-001 待 PM 确认；PRD 风险 2/3 的平台只读探测待 Owner 授权；归 PM 的 deep-dive §8 订正。
  - **B3（Demo 轮询 `"0" == 0` 类型 bug）仍未入 `docs/knowledge/engineering/`** ——连续四轮记在遗留项里未清，v0.2 做仿真调度前必须落。设计阶段已关闭，建议实现阶段或迭代收尾时处理。
- 下一步入口：新开会话切 **Developer 启动实现阶段**（必读设计文档 §3 契约 + §8 交接清单）。我在实现阶段转 Review 方，由 Developer 送审时指定。
- 收尾状态：已收尾
