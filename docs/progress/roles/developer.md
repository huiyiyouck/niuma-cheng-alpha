# Developer（开发工程师）角色日志

## 2026-08-15（续 5）— v0.1 设计文档 R4 快速复核
- 本次角色：Developer（开发工程师）
- 动作：Review（v0.1 设计文档 R4 快速复核，按 Owner 裁决只核 R3 意见落点）
- 涉及文档：`iterations/v0.1-design.md`（追加 R4 复核记录 + Review 状态表本方行）、`iterations/v0.1.md`（设计阶段 R4 门禁行 Developer 列）、`INDEX.md`
- 结论：**通过**。D21≡N14 已闭合——§3.1 补 `get_alpha(alpha_id)`（`GET /alphas/{id}`，Demo 与平台事实双重确认）、§4.6 收敛改逐条直查并删掉「先增量同步」那一步，判据现在完全绕开本地库与水位；N15（查询失败保持 `in_flight` 不动）、D22（80 秒带出处）、D23（守卫收窄到 `status_source='prediction'`）、Q7（`is_pending_flag` 归预判侧 + §4.5 口径限定）全部落地；§8 第 6 条扩为六条并附针对性用例。未见改动溢出到 R3 意见范围之外。新增 D24 低——reconcile 收敛只写 `alpha_state`、未顺手 UPSERT `alpha` 表的 `stage`，当前逻辑均按 `funnel_status` 走故无功能影响，可留实现阶段。
- 关键取舍（选了什么 / 备选什么 / 为什么）：严格按 Owner 裁决的快速通道只核落点，没有借复核之机重审全文——设计连续四轮、两方共提 24 项意见，此时继续扩大审查面只会推迟定稿而边际收益递减；D24 判低并明确「可留实现阶段」，不给定稿再加一道门。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：`CN-001`（仍待 PM 确认，不阻塞设计定稿）
- 遗留问题/风险：§8 第 6 条那份「亮绿灯但数据错」清单现有六条，是本项目最有价值的一份实现期检查表——实现阶段自测报告应按该清单逐条留证据，尤其第 6 条要求替身模拟出「常规链路拿不到、直查能拿到」的差异，替身若按理想情况构造会把取数路径的错误一并掩盖。
- 下一步入口：Architect 定稿设计（顺手处置 D24 与 DevOps N16 两条低）→ Owner 确认 + 降级留痕 → 进实现阶段，由 Developer 承接实现（届时先按 §8 交接清单建骨架：`db.py` schema → `client.py` 含替身注入点 → 其余模块）。
- 收尾状态：未收尾

## 2026-08-15（续 4）— v0.1 设计文档 R3 复核
- 本次角色：Developer（开发工程师）
- 动作：Review（v0.1 设计文档 R3 复核）
- 涉及文档：`iterations/v0.1-design.md`（追加 R3 复核记录 + Review 状态表本方行）、`iterations/v0.1.md`（设计阶段 R3 门禁行 Developer 列）、`INDEX.md`（含顺手同步 R-1 耗时数字 100s→80s，照 R3 已订正值，非结论改写）
- 结论：**需修改**。R2 的 D16-D20 全部核销，无驳回；D16 的修法优于我的建议（我给的是补 `ifnull()`，Architect 把判断整体移出 SQL，消除同类问题的土壤）。新增 **D21 高**——`submit --reconcile` 的收敛第一步写「先 `sync --incremental --stage OS` 刷新平台字段」，但增量是按 `dateCreated > 水位` 拉新增，一条被提交的 Alpha `stage` 由 IS 变 OS 而 `dateCreated` 不变、远低于水位，增量永远拉不到它（§4.2 与 PRD #2 定稿口径亦明写「已入库记录的字段刷新不属增量职责」）；于是收敛读到库里旧值 `IS` → 判「提交未达成」→ 回退「待确认」→ 下次 `submit` 再次 POST，正是 N8/D19 整条机制要防的二次不可逆动作。且 `BrainClient` 契约无按 `alpha_id` 查单条的方法，该命令在当前契约下无法实现。另 D22 低（§4.4 仍写 100 秒，与 R-1 订正后的 80 秒不一致）、D23 低（`--reset` 守卫可再收窄一档）、Q7 待澄清（`is_pending_flag` 归属哪一侧）。
- 关键取舍（选了什么 / 备选什么 / 为什么）：判「需修改」而非「通过（附条件）」——D21 落在唯一不可逆动作的恢复路径上，且是 R3 新增机制自带的缺陷；但同时在放行条件里声明「按建议修订并在 R4 说明列出改动即可快速复核放行，不重审全文」，既守住门禁又不让轮次空转。备选是直接给附条件通过、把处置压到定稿动作里（PRD 阶段有此先例），未采纳：那条先例处置的是文本与口径，这次处置的是会导致重复提交的执行路径。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：`CN-001`（仍待 PM 确认）
- 遗留问题/风险：D21 修法需给 `BrainClient` 增加 `get_alpha(alpha_id)`（`GET /alphas/{id}`，Demo 的 `get_alpha_details` 即此），这是 R1 以来第一次扩客户端契约面——实现阶段的替身也要相应覆盖该方法，否则 `--reconcile` 无法自动验证。
- 下一步入口：Architect 处置 D21（必修）+ D22/D23/Q7 → Developer 快速复核；DevOps R3 复核仍待另开会话 → 两方通过后定稿进实现阶段（降级模式需 Owner 确认 + 留痕）。
- 收尾状态：未收尾

## 2026-08-15（续 3）— v0.1 设计文档 R2 复核
- 本次角色：Developer（开发工程师）
- 动作：Review（v0.1 设计文档 R2 复核）
- 涉及文档：`iterations/v0.1-design.md`（追加 R2 复核记录 + Review 状态表本方行）、`iterations/v0.1.md`（设计阶段 R2 门禁行 Developer 列）、`INDEX.md`
- 结论：**需修改**。R1 的 D9-D15 与 Q5/Q6 全部核销、无驳回（D10 的解法优于我原建议：让 `funnel_status` 从根上恒非空，而非在 WHERE 里对 NULL 打补丁）。新增 **D16 高**——D14 的修法 `AND NOT (funnel_status='改造候选' AND prediction_result='fail')` 踩了 D10 同型 NULL 陷阱，且方向恰好相反：`prediction_result` 为 NULL 时整表达式求值为 NULL，被 WHERE 静默丢弃，因此排除掉的是「从未预判的改造候选」（主体），而非意图排除的「预判判死的改造候选」；后果是 `classify --threshold` 调整后绝大多数改造候选不参与重算、验收 #5 对改造候选整体失效，而守恒断言仍成立 → 又一条亮绿灯的失败路径。另 D17 中（§4.3 正文「理由照常刷新」与 SQL 排除该类记录自相矛盾）、D18 中（`--reset` 无括号无保护态守卫，可把已提交记录拖回待确认）、D19 中（`in_flight` 无解除路径，同 DevOps N11）、D20 低。
- 关键取舍（选了什么 / 备选什么 / 为什么）：D16 不靠读代码下结论，先用 SQLite 内存库跑五条样本实机验证三值逻辑（`('改造候选', NULL)` 被排除、`('淘汰候选', NULL)` 因 `FALSE AND NULL = FALSE` 短路不受影响），再写进 Review——同型问题上一轮已判过一次，重复指认必须有证据而非印象。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：`CN-001`（D12 触发，Architect 已建、待 PM 确认）
- 遗留问题/风险：D9/D10/D16 三条同型——**缺陷以绿灯形态通过验收**。设计已在 §8 第 6 条把这类路径单列为重点自测项，建议 D16 一并并入该清单；降级模式无 CI 兜底，实现阶段这类问题只能靠针对性用例覆盖，自测报告里应能看到「改阈值重跑 classify，改造候选归属随之变化」这条用例。
- 下一步入口：Architect 修订出 R3（D16 必修、D17 择一写死、D18/D20，D19 与 N11 合并）→ 两方复核 → 定稿进实现阶段（降级模式需 Owner 确认 + 留痕）。
- 收尾状态：未收尾

## 2026-08-15（续 2）— v0.1 设计文档 R1 Review
- 本次角色：Developer（开发工程师）
- 动作：Review（v0.1 设计文档 R1，接口契约可实现性与工程成本边界）
- 涉及文档：`iterations/v0.1-design.md`（追加 R1 Developer Review 记录 + Review 状态表本方行）、`iterations/v0.1.md`（设计阶段 R1 门禁行 Developer 列）、`INDEX.md`
- 结论：**需修改**。两项高：D9——§4.1 用 `dateCreated>from` / `<to` 开区间切片，但初始边界直接取首末条时间戳，致 `T_min`/`T_max` 同秒记录全部漏拉；且窗口级对账的左值 `reported_count` 与右值 `fetched_count` 出自同一开区间查询，左右值同源，缺失记录两边都不出现 → 验收 #1 在数据实际缺失时仍全绿。D10——设计要求「同步只写 `alpha` 表」，而 PRD §3.1 要求「IS→已回测 / OS→已提交」为同步入库的初始态，两者不可兼得；叠加 §4.3 适用域 `funnel_status NOT IN (...)` 对 NULL 求值为 NULL，无派生行的记录被静默排除，验收 #4「无未分类残留」同样查不出（被排除者不在结果集内）。另 D11-D15 中（递归切片窄窗口不收敛、#1 口径变更未走 Change Note、替身注入粒度与 client 依赖注入无契约、分流重算抹掉预判在状态层的表达、待定记录无批量重跑且无按状态查询入口）、Q5/Q6 待澄清。工程成本与 PRD 阶段估算一致，不构成阻塞。
- 关键取舍（选了什么 / 备选什么 / 为什么）：D9/D10 判为「高 + 需修改」而非「中 + 附条件通过」——两者共同特征是**缺陷会以绿灯形态通过验收**，而 Owner 已裁决 v0.1 全程降级模式、无 CI 兜底，这类问题一旦进实现阶段几乎不可能被发现。备选是跟 DevOps 一样给附条件通过、把处置压到实现阶段，未采纳。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：无（D12 建议 Architect 或 PM 就 PRD #1 主口径变更补一份轻量 Change Note）
- 遗留问题/风险：D13（业务模块如何取得 `BrainClient`、替身注入粒度）若不在设计定稿前钉死，按模块并行铺开实现时三个隔离会话会各自发明注入方式，验收 #8「提交接口零调用」的断言写法也会分叉——这是进实现阶段前最该先合上的口子。
- 下一步入口：Architect 修订设计出 R2（D9/D10 必修，D11-D15 + Q5/Q6、DevOps N5-N9 一并处置）→ Developer / DevOps 复核 → 定稿进实现阶段（降级模式需 Owner 确认 + 留痕）。
- 收尾状态：未收尾

## 2026-08-15（续）— v0.1 PRD R2 复核
- 本次角色：Developer（开发工程师）
- 动作：Review（v0.1 PRD R2 复核）
- 涉及文档：`iterations/v0.1-prd.md`（追加 R2 复核记录 + 更新 Review 状态表本方行）、`iterations/v0.1.md`（R2 门禁行 Review 结果）、`INDEX.md`
- 结论：**通过（附条件）**。R1 的 D1-D4、Q1-Q4 全部核销——D1 由新增 §3.2 规则表解决（门槛项 = PASS/FAIL，其余非门槛留痕；gap 公式 + 改造阈值 0.3 可配；三判定互斥完备），D2 改双路对账，D3 删「未回测」级，D4 升级为验收 #10，Q1-Q4 答案均已回填正文。新增 D5（中：重新分流的适用域与保护态未定义，与验收 #5「重新分流生效」冲突，且「待确认/已提交/提交失败」被覆盖有实际损害）、D6（中：验收 #1 通过条件二义，断言写不出确定期望值）、D7/D8（低：gap 除零兜底、提交清单取数状态）。均不阻塞定稿。
- 关键取舍（选了什么 / 备选什么 / 为什么）：D5/D6 判为「中 + 附条件通过」而非再要一轮 R3——两条都是补一句口径的成本，且属数据模型与同步策略范畴，设计阶段 Architect 必然要落定；再压一轮 R3 只增加轮次不增加信息。备选是判高并要求 R3，未采纳。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：无
- 遗留问题/风险：D5 若在设计阶段仍未落定，实现时会以「分流只跑一次」的语义写死，导致验收 #5 无法通过——这是本轮最需要盯住的一条。
- 下一步入口：PM 处置 D5/D6（补正文 或 明确延期记录）；Architect R2 复核仍待另开会话冷启动完成 → 全通过后 PM 定稿进设计阶段（降级模式需 Owner 确认 + 留痕）。
- 收尾状态：未收尾

## 2026-08-15 — 会话摘要
- 本次角色：Developer（开发工程师）
- 动作：Review（v0.1 PRD R1，工程成本与可行性边界）
- 涉及文档：`iterations/v0.1-prd.md`（追加 Review 记录 + 更新 Review 状态表本方行）、`iterations/v0.1.md`（门禁表 Review 结果）、`INDEX.md`
- 结论：**需修改**。缺陷 4 项——D1（高）三分类判定规则全文未定义，致验收 #4/#5 无法实现与断言；D2（中）验收 #1「平台实际总数」口径不可判定；D3（中）报告漏斗级「未回测」在 v0.1 无数据源；D4（低）全量同步无断点续传（不阻塞定稿，交设计阶段）。待澄清 4 项 Q1-Q4：增量同步的变更判定键、同步幂等与本地派生字段回写的覆盖关系、人工确认标记的载体与粒度、PENDING 类检查的异步终止条件。工程成本本身可控（5 模块、单迭代体量合理），不构成阻塞。
- 关键取舍（选了什么 / 备选什么 / 为什么）：把「三分类规则缺失」判为缺陷·高而非待澄清问题——它不是表述含糊，而是产品决策缺项，若按待澄清走会让实现方替 PM 定规则；备选是降为中并放行到设计阶段补，未采纳，因为验收 #4/#5 直接依赖它，放行等于让不可验证的验收标准定稿。
- 关联迭代：v0.1
- 关联非迭代工作：无
- 关联 Change Note：无
- 遗留问题/风险：验收 #2/#4/#5/#8/#9 要求自动化验证，实现前提是平台 API 访问收敛到可注入替身的单一客户端层——已写入 Review 记录，提请设计阶段确认（非 PRD 缺陷）。
- 下一步入口：PM 会话修订 PRD 出 R2（回填 Q1-Q4 答案入正文）→ 另开会话由 Developer 复核；Architect 的 R1 Review 仍待反馈。
- 收尾状态：未收尾
