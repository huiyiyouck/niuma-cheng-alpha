# v0.1 设计文档

## 文档状态
- 整体状态：待Review
- Review 轮次：R1
- 关联 PRD：`iterations/v0.1-prd.md`（已定稿 2026-08-15，R2 三方通过）

## Review 计划
- Review 模式：动态 Review
- 影响领域：接口契约与实现成本（开发）、运行产物 / 凭据 / 长任务韧性（运维）
- 本轮指定 Review 方：
  - Developer（开发工程师）——数据模型与接口契约能否无歧义实现、模块划分与工程成本、可测性（#5/#6/#8/#9 的替身注入前提）
  - DevOps（运维/部署工程师）——运行产物目录与 git 忽略（N2）、断点状态生命周期（N3）、一键验证入口（N4）、凭据加载与认证自愈、长任务韧性
- 未指定角色及原因：
  - PM——本设计未偏离 PRD 定稿口径，无产品范围变更；§7 风险 R-1（预判耗时量级）为**提请 Owner 知悉**事项，若 Owner 据此决定收窄范围，再按 Change Note / 回阶段规则处理，不作为本轮 Review 门禁
- 少于 2 个 Review 方时的用户确认：无（已指定 2 方）
- 定稿条件：本轮指定 Review 方全部通过，且无未处理高严重度阻塞问题

## Review 状态
| Review 方 | 指定原因 | 状态 | 轮次 |
|-----------|----------|------|------|
| Developer | 接口契约可实现性与工程成本 | 需修改（D9/D10 高） | R1 |
| DevOps | 运行产物、断点生命周期、验证入口、韧性 | 待反馈 | R1 |

---

## 1. 概述

### 1.1 设计目标与边界

实现 PRD v0.1 定稿的漏斗右半段闭环：**同步 → 分流 → 预判 → 报告 → 确认提交**。五条命令、一个本地库、一个 HTTP 出口。

本设计只解决 PRD 范围内的问题。不为 v0.2（改造回路 / 常驻调度）和 v0.3（组装 / Web）预建抽象，只在**代价为零的地方**留出接口边界（§6.2 列明是哪几处，以及为什么这几处不算过度设计）。

### 1.2 技术选型

| 维度 | 选型 | 理由 |
|------|------|------|
| 语言 | Python 3.11+ | 平台调研与 Demo 均为 Python，`requests` 生态与已验证的调用面可直接复用；Owner 侧无第二语言运行时负担 |
| 存储 | SQLite（标准库 `sqlite3`） | 1 万+ 条 × 完整 JSON 的量级下仍是单文件、零运维；事务保证断点续跑的一致性；「备份 = 复制单文件」正是 PRD 设计约束（M4）要的形态 |
| HTTP | `requests` | Demo 已验证的调用面（认证 / 列表 / 相关性 / 提交）全部基于它；v0.1 全串行，不需要异步 |
| CLI | 标准库 `argparse` | 五条子命令，无需第三方框架 |
| 数据结构 | 标准库 `dataclasses` | 不引入 pydantic：v0.1 的外部输入只有平台 JSON，校验点集中在客户端层，运行时模型校验收益不抵依赖成本 |

**运行时依赖只有 `requests` 一个**（测试期另加 `pytest`）。选型详见 `docs/knowledge/decisions/ADR-0001-技术栈与存储选型.md`。

### 1.3 模块地图

```text
alpha_platform/
├── cli.py          子命令入口、参数解析、退出码（§3.2 契约）
├── config.py       配置解析：默认值 → 配置文件 → 环境变量 → 命令行参数（后者覆盖前者）
├── credentials.py  凭据加载（BRAIN_CREDENTIALS_PATH 可覆盖，默认项目根）
├── client.py       BrainClient：★ 全项目唯一 HTTP 出口，可注入替身（§3.1 契约）
├── db.py           连接管理、schema 建表、schema_version 校验
├── sync.py         全量（时间窗自适应切片）/ 增量同步、窗口级对账、断点
├── classify.py     分流引擎：核心为纯函数，不碰 IO
├── precheck.py     提交前预判：两阶段短路、分批、跳过已有结论
├── report.py       漏斗报告（text / json）
└── submit.py       dry-run / 指纹确认 / 提交 / 快照落盘
```

模块划分对齐 Developer R1 估算的五个可独立自测单元（客户端 / 同步 / 分流 / 预判 / 报告+提交），另拆出 `db`、`config`、`credentials` 三个无业务逻辑的基础件。

### 1.4 运行产物布局（DevOps N2）

```text
data/                       ← 统一运行产物目录，整目录 git 忽略
├── inventory.db            库存库（SQLite；含库存、派生状态、断点）
└── snapshots/              提交清单快照，一次提交一个文件
    └── 20260815-143022-a3f1b8c2.json
```

实现阶段首个 commit 前，`.gitignore` 追加 `data/`。快照落文件而非入库：它是**归档证据**，需要能被单独复制、审计、离线保留，不应随库文件重算或迁移而变动。

---

## 2. 数据模型

库文件：`data/inventory.db`。四张表。字段分层（PRD §3.1 Q2/M4）**由 schema 强制**而非靠代码纪律——同步流程只写 `alpha` 表，物理上碰不到派生数据。

### 2.1 `alpha`（平台原始字段，同步 UPSERT，不可变语义）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `alpha_id` | TEXT | PRIMARY KEY | 平台 Alpha ID |
| `stage` | TEXT | NOT NULL | `IS`（未提交）/ `OS`（已提交） |
| `date_created` | TEXT | NOT NULL, INDEX | ISO8601；增量水位与时间窗切片的键 |
| `date_submitted` | TEXT | NULL | 仅 OS 有值 |
| `expression` | TEXT | NOT NULL | 表达式（`regular.code`） |
| `raw_json` | TEXT | NOT NULL | ★ 列表项完整响应原样保存（含 settings / is 指标 / is.checks） |
| `fetched_at` | TEXT | NOT NULL | 本次拉取时间（UTC ISO8601） |

**设计取舍：settings / metrics / checks 不单独建列，只存 `raw_json` 一份。** 平台列表 API 每项自带完整 settings + code + is 指标 + checks（`brain-platform-facts.md` §4），拆列会让同一份数据在库中存在两处、可能不一致，而「平台原始字段不可变」要的恰恰是单一保真源。`checks` 每次分流时从 `raw_json` 解析——1 万条 JSON 解析在手动触发的命令里是秒级，不值得用一致性风险去换。上方五个抽出列只服务于**索引与查询**（主键、增量水位、状态查询），不构成第二份真源。

### 2.2 `alpha_state`（本地派生字段，同步永不触碰）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `alpha_id` | TEXT | PK, FK→`alpha` | |
| `funnel_status` | TEXT | NOT NULL, INDEX | PRD §3.1 七态之一 |
| `is_pending_flag` | INTEGER | NOT NULL DEFAULT 0 | 预判「待定」标注（PRD §3.1：待定不是独立状态，是「可提交候选」上的标注） |
| `classified_at` | TEXT | NULL | |
| `classify_threshold` | REAL | NULL | ★ A10：本次分流使用的改造阈值 |
| `classify_rule_version` | TEXT | NULL | ★ A10：分流规则版本号 |
| `classify_reason_json` | TEXT | NULL | 分流理由明细（§2.5 结构） |
| `prediction_result` | TEXT | NULL | `pass` / `fail` / `pending`；★ 非空即「已有预判结论」，A9 的跳过判据 |
| `prediction_reason_json` | TEXT | NULL | 相关性值、阈值、阈值来源（§2.5 结构） |
| `predicted_at` | TEXT | NULL | |
| `confirm_fingerprint` | TEXT | NULL | 确认时绑定的清单指纹 |
| `confirmed_at` | TEXT | NULL | |
| `submitted_at` | TEXT | NULL | |
| `submit_result` | TEXT | NULL | `success` / `failed` |
| `submit_error` | TEXT | NULL | 失败原因原文（Demo 弱点 2 的反面：不吞异常） |

`classify_threshold` 与 `classify_rule_version` 既单独建列又出现在 `classify_reason_json` 里：单列供 SQL 直接筛选与断言（「用旧规则版本分流的还剩多少条」），JSON 存完整明细。这处冗余是有意的，两者同一次写入、不存在漂移窗口。

### 2.3 `sync_slice`（同步断点，临时派生，可安全删除）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `stage` | TEXT | PK 组成 | `IS` / `OS` |
| `slice_from` | TEXT | PK 组成 | 窗口起（用作 `dateCreated>`） |
| `slice_to` | TEXT | PK 组成 | 窗口止（用作 `dateCreated<`） |
| `reported_count` | INTEGER | NOT NULL | 平台对该窗口报告的 `count`（窗口级对账的左值） |
| `fetched_count` | INTEGER | NOT NULL | 该窗口实际拉取到的去重条数（右值） |
| `status` | TEXT | NOT NULL | `done` / `partial` |
| `completed_at` | TEXT | NULL | |

**生命周期（DevOps N3）**：本表是**临时派生数据**，不属于 §3.1 的字段分层两类中的任何一类。可随时清空；清空后果仅为「下次全量同步从头切片重跑」，因入库为 UPSERT，不产生重复记录、不影响库存正确性与派生数据重算能力。备份时可不含本表。

### 2.4 `meta`（键值对）

| 键 | 说明 |
|----|------|
| `schema_version` | schema 版本号，启动时校验 |
| `last_sync_date_created:{stage}` | 增量同步水位（该 stage 已同步到的最大 `date_created`） |
| `last_full_sync_at` | 上次全量同步完成时间 |
| `offset_ceiling_detected` | #1 诊断项留痕：`{"detected": true, "offset_count": 10000, "slice_count": 12841, "at": "..."}` |

**预判进度不建表**：`alpha_state.prediction_result` 非空即代表「该条已有结论」，预判命令天然跳过——PRD #6 的「断点续跑」与「已获取结果不重复请求」是同一件事，用同一个字段表达，不引入第二个状态载体。

### 2.5 JSON 字段结构

`classify_reason_json`：
```json
{
  "rule_version": "v1",
  "threshold": 0.3,
  "verdict": "改造候选",
  "gate_fails": [
    {"name": "LOW_SHARPE", "value": 1.21, "limit": 1.58, "gap": 0.234, "quantifiable": true}
  ],
  "gate_passes": ["LOW_FITNESS", "LOW_TURNOVER"],
  "non_gate": [
    {"name": "CLUSTER_TEST", "result": "ERROR"},
    {"name": "UNITS", "result": "WARNING"}
  ]
}
```

`prediction_reason_json`：
```json
{
  "verdict": "fail",
  "stage_reached": "self",
  "self_correlation": 0.83,
  "prod_correlation": null,
  "threshold": {"value": 0.7, "source": "convention_demo"},
  "attempts": 1,
  "short_circuited": true
}
```

`threshold.source` 取值：`platform`（平台 checks 返回的 limit）/ `convention_demo`（Demo 沿用的 0.7 惯例值）/ `manual`（配置文件手工指定）。PRD #6 要求阈值必带来源、不允许无来源硬编码——该字段是可审计凭证，写入每一条预判结论。

---

## 3. 接口契约

> 本节是隔离会话之间的交接带宽。实现会话只读此处，不回问上游。

### 3.1 `BrainClient` —— 全项目唯一 HTTP 出口

PRD §5 设计约束要求「平台 API 访问收敛到可注入替身的单一客户端层」。**除本类外，任何模块不得直接发起 HTTP 请求**；这是 #5 / #6 / #8 / #9 能在不触达真实平台的前提下自动验证的唯一前提。

```python
@dataclass(frozen=True)
class Credentials:
    email: str
    password: str          # 仅存在于内存；永不写回任何文件（Demo 弱点 1 的反面）

@dataclass(frozen=True)
class AlphaListPage:
    count: int             # 平台对该查询条件报告的总数
    items: list[dict]      # 原样 JSON 列表项

@dataclass(frozen=True)
class CorrelationResult:
    max_correlation: float | None   # None = 重试耗尽仍无数据
    attempts: int
    raw: dict | None

@dataclass(frozen=True)
class SubmitOutcome:
    ok: bool
    error: str | None      # 失败原因原文，不吞异常
    raw: dict | None

class BrainClient:
    def __init__(self, credentials: Credentials, *,
                 session: requests.Session | None = None,
                 base_url: str = "https://api.worldquantbrain.com",
                 retry: RetryPolicy | None = None) -> None: ...

    def list_alphas(self, *, stage: str, limit: int, offset: int,
                    date_created_gt: str | None = None,
                    date_created_lt: str | None = None,
                    order: str = "dateCreated") -> AlphaListPage: ...

    def get_self_correlation(self, alpha_id: str) -> CorrelationResult: ...
    def get_prod_correlation(self, alpha_id: str) -> CorrelationResult: ...
    def submit_alpha(self, alpha_id: str) -> SubmitOutcome: ...
```

**平台端点映射**（均已在 Demo 中验证存在）：

| 方法 | HTTP | 路径 | 备注 |
|------|------|------|------|
| `list_alphas` | GET | `/users/self/alphas` | 参数 `stage / limit / offset / order`，时间窗用 `dateCreated>` `dateCreated<` |
| `get_self_correlation` | GET | `/alphas/{id}/correlations/self` | 平台异步计算，首查常返空 |
| `get_prod_correlation` | GET | `/alphas/{id}/correlations/prod` | 同上 |
| `submit_alpha` | POST | `/alphas/{id}/submit` | ★ 唯一不可逆动作 |
| （内部）认证 | POST | `/authentication` | Basic Auth → JWT 存 cookie `t` |
| （内部）探活 | GET | `/authentication` | 见 §3.1.1 |

#### 3.1.1 认证自愈

每个公开方法进入时先 `_ensure_authenticated()`：检查 cookie 存在 → GET `/authentication` 探活 → 失效则用内存中的凭据重新认证。这直接采纳 Demo 已验证的模式（`demo-code-deep-dive.md` §3.1），**并因此关闭 PRD 风险 5 的 session 半边**：无需实测 session 生命周期，按需重认证即可覆盖长任务中途失效。

与 Demo 的差异（凭据纪律）：凭据只从 `.brain_credentials` 读入内存，**绝不回写任何文件**；日志中不出现 email / password / Authorization 头 / cookie 值。Demo 认证成功后回写明文凭据到 `user_config.json`，是我们明确不采纳的做法。

生物识别分支（Demo 用 playwright 开浏览器扫脸）**不实现**：v0.1 遇 `WWW-Authenticate: persona` 直接抛 `AuthError` 并提示 Owner 手工处理，不引入浏览器依赖。

#### 3.1.2 重试与退避策略

```python
@dataclass(frozen=True)
class RetryPolicy:
    correlation_max_attempts: int = 5      # 相关性接口
    correlation_interval_sec: float = 20.0 # 固定间隔（Demo 实测值）
    list_max_attempts: int = 3
    list_backoff_base_sec: float = 2.0     # 指数退避 ×1.5
    rate_limit_backoff_sec: float = 30.0
```

参数来自 Demo 实测（`demo-code-deep-dive.md` §3.4/§3.5：相关性 5 次 × 固定 20s，记录集 5 次 × 2s 指数退避）。全部可配置——这些是**平台行为的观测值，不是常量**，平台变化时应能不改代码调整。

#### 3.1.3 异常契约

| 异常 | 触发条件 | 上层处置 |
|------|----------|----------|
| `AuthError` | 凭据缺失 / 认证失败 / 需生物识别 | 立即终止，退出码 2（PRD #9） |
| `RateLimitError` | HTTP 429 或平台并发限制响应 | 退避后重试；重试耗尽则中止当前批次，断点保留 |
| `PlatformError` | 其他 4xx/5xx | 记录并中止当前条目，不影响其余条目 |
| （无异常） | 相关性重试耗尽 | 返回 `max_correlation=None`，由 `precheck` 判为「待定」，不抛异常 |

### 3.2 CLI 契约

统一入口 `python -m alpha_platform <command>`。

| 命令 | 参数 | 行为 |
|------|------|------|
| `sync --full` | `--stage {IS,OS,all}`（默认 all）、`--resume`（默认开）、`--split-threshold N`（默认 9000） | 时间窗自适应切片全量同步 + 窗口级对账 + offset 封顶诊断 |
| `sync --incremental` | `--stage {IS,OS,all}` | 按 `meta` 水位拉新增 |
| `classify` | `--threshold F`（默认 0.3）、`--scope {all,unclassified}`（默认 all） | 对适用域重算分流；`all` 含已分流三类候选，保护态永不参与 |
| `precheck` | `--batch-size N`（默认 20）、`--limit N`（本次最多处理条数）、`--reset ALPHA_ID...` | 对「可提交候选且无预判结论」执行两阶段预判 |
| `report` | `--format {text,json}`（默认 text）、`--output PATH` | 漏斗报告 |
| `submit --dry-run` | — | 打印待确认清单与指纹；**提交接口零调用** |
| `submit --confirm FP` | — | 校验指纹后逐条提交、回写、落快照 |

**退出码**：`0` 成功 / `1` 一般失败 / `2` 认证失败 / `3` 参数错误 / `4` 指纹失效或不匹配 / `5` 中断可续跑（断点已保存）。

区分 `4` 与 `5` 的原因：前者要求人重新确认（安全语义），后者只需重跑同一命令（韧性语义），Owner 与脚本都需要能不看日志就分辨。

---

## 4. 核心流程

### 4.1 全量同步：时间窗自适应切片（PRD #1 / #10）

PRD 主口径是「本地去重条数 == 时间窗切片穷尽求和的去重条数」。**若切片是唯一写入路径，这两个数字恒等，验收会退化回 R1 A4 指出的自证循环。** 因此本设计把主口径落到**窗口级对账**上——它才是可证伪的那一层：

```text
1. 定边界：limit=1 分别按 order=dateCreated / -dateCreated 取首末条 → [T_min, T_max]
2. 递归切片 split(from, to):
     page = list_alphas(stage, limit=1, offset=0, dateCreated>from, dateCreated<to)
     若 page.count >= split_threshold(9000)：
         mid = from + (to - from) / 2
         split(from, mid) ; split(mid - 1s, to)      ← 重叠 1 秒，见下
     否则：该窗口为叶子 → 记 reported_count = page.count → 进入步骤 3
3. 叶子窗口穷尽：offset 从 0 步进 limit=100 拉完 count 条 → UPSERT 入 alpha 表
   → 记 fetched_count = 该窗口去重 alpha_id 数 → sync_slice.status = done
4. 窗口级对账（★ 主口径断言）：每个叶子窗口 fetched_count == reported_count
5. 幂等断言：连续两次全量同步，alpha 表行数不变
6. offset 封顶诊断：另跑一次不带时间窗的 offset 直连穷尽 → offset_count
   若 offset_count != 本地行数 → 写 meta.offset_ceiling_detected 留痕（断言留痕存在，不断言两数相等）
```

**边界重叠 1 秒的原因**：平台参数是 `dateCreated>` 与 `dateCreated<`，两端都是**开区间**。若相邻窗口首尾相接，恰好等于切分点的记录会被两侧同时排除而**静默丢失**。让下一窗口起点回退 1 秒，边界记录被拉两次，再由 `alpha_id` 主键 UPSERT 去重——PRD 主口径写的正是「**去重**条数」，与该策略天然契合。

**为什么用 `count` 而不是拉完才知道**：列表 API 响应带 `count` 字段（`brain-platform-facts.md` §1 即由此得出「未提交 10000」），`limit=1` 一次请求即可判断是否需要细分，无需试探性拉取。

**递归终止**：切到 `from == to`（同一秒）仍 `count >= 阈值` 时，标记该窗口 `status=partial` 并留痕，用 offset 尽力拉取。实测样本创建时间集中在 2026-01-24~02-01（批量生成产物），单日过万并非不可能，因此切分按**时间戳**而非按天，可一路细分到秒级。

**断点（#10）**：`sync_slice` 中 `status=done` 的窗口在 `--resume` 时直接跳过。中断后重跑只重做未完成窗口。

### 4.2 增量同步（PRD #2）

读 `meta.last_sync_date_created:{stage}` 作为水位，`dateCreated>水位` 拉取，UPSERT，更新水位。增量语义严格等于「新增」——已入库 Alpha 的平台字段刷新由全量重拉或预判回写承担（PRD #2 定稿口径）。幂等由 UPSERT + 水位单调保证。

### 4.3 分流（PRD #4 / #5，§3.2 规则）

核心是**纯函数**，不碰 IO——这是 #5「注入不同 limit 的 checks 数据断言分流结果变化」能被单测覆盖的前提：

```python
RULE_VERSION = "v1"

def classify_checks(checks: list[dict], threshold: float) -> ClassifyResult:
    """输入 is.checks[] 与改造阈值，输出状态与理由。无 IO、无全局状态、可重复调用。"""
```

判定逻辑严格照 PRD §3.2：

1. 门槛项 = `result ∈ {PASS, FAIL}`；`WARNING / ERROR / PENDING` 一律非门槛，进 `non_gate` 留痕但不参与判定
2. `gap = |value − limit| / |limit|`；`value` 或 `limit` 缺省、或 `limit == 0` → 标 `quantifiable: false`，按「差距在阈值内」处理（保守归改造候选）
3. 无 FAIL → 可提交候选；全部可量化 FAIL 的 gap ≤ 阈值 → 改造候选；任一 > 阈值 → 淘汰候选

**适用域（PRD §3.2 D5/A11）**：`funnel_status NOT IN ('待确认','已提交','提交失败')`。重算只覆盖 `funnel_status` 与 `classify_*` 字段，**不触碰 `prediction_*`**（A9）——被预判打回「改造候选」的记录即使被重算回「可提交候选」，其预判结论仍在，`precheck` 会跳过它，不会二次消耗相关性配额。

每次写入必带 `classify_threshold` 与 `classify_rule_version`（A10）：任一历史分流结论都能被解释和重现。

### 4.4 提交前预判：两阶段短路（PRD #6）

**成本是本流程的核心约束。** 相关性接口最坏 5 次重试 × 20s = 100 秒/条（Demo 实测参数）。据此设计两阶段短路：

```text
候选集 = funnel_status == '可提交候选' AND prediction_result IS NULL     ← A9 跳过已有结论
按 --batch-size 分批，逐条：

  阶段一：get_self_correlation(alpha_id)
    max_corr is None（重试耗尽） → prediction_result = 'pending'
                                   funnel_status 保持「可提交候选」+ is_pending_flag = 1
                                   继续下一条（不阻塞其余候选）
    max_corr >= threshold        → prediction_result = 'fail'
                                   funnel_status = '改造候选'
                                   ★ 不调用 prod 接口（短路）
    max_corr <  threshold        → 进入阶段二

  阶段二：get_prod_correlation(alpha_id)
    同上三分支；两路均低于阈值 → prediction_result = 'pass'，funnel_status = '待确认'

每条结论立即写库并提交事务 → 任意时刻中断，已完成部分不丢，重跑自动跳过
```

**短路的依据**：Demo 的 `brain-calculate-alpha-selfcorrQuick` skill 明确记载「self-corr > 0.7 则 prod-corr 必然 > 0.7」（`demo-code-deep-dive.md` §5 重点 2）。self 超阈即可直接判死，无需再查 prod。这把最坏情况下的平台调用次数**减半**，且不偏离 PRD #6 的「self/prod 相关性接口」路径。

阈值与来源写入每条 `prediction_reason_json`（§2.5）。v0.1 默认 `{"value": 0.7, "source": "convention_demo"}`，可经配置改为 `manual`；若日后探明平台返回该项 limit，改为 `platform` 而不动上层逻辑。

### 4.5 漏斗报告（PRD #7）

只读，纯 SQL 聚合，三块内容：

1. **各状态数量**：按 §3.1 七态分组计数（不含「未回测」——v0.1 无数据源）
2. **FAIL 卡点分布 TOP 榜**：从 `classify_reason_json.gate_fails` 展开，按 check 名聚合出现次数与平均 gap
3. **携带未判定门槛类 check 的候选数（A12）**：`non_gate` 中含 `ERROR` / `PENDING` 的「可提交候选」条数。CLUSTER_TEST 全样本为 ERROR，若它日后被证实是硬门槛，当前分流整体偏乐观——提交机会稀缺，这个数字必须对 Owner 可见

### 4.6 提交（PRD #8）—— 唯一不可逆动作

```text
submit --dry-run:
  清单 = funnel_status == '待确认' 的全部记录（PRD D8 口径）
  指纹 = sha256("\n".join(f"{alpha_id}:{prediction_result}" for 按 alpha_id 升序))
  打印清单 + 指纹 → 退出。★ 提交接口零调用（可断言）

submit --confirm <fingerprint>:
  重新计算当前清单指纹
  不匹配 → 退出码 4，拒绝提交（清单内容已变，确认失效）
  匹配 → 逐条 POST /alphas/{id}/submit
         成功：funnel_status = '已提交'，submitted_at、submit_result = 'success'
         失败：funnel_status = '提交失败'，submit_error = 原因原文（不吞异常）
         每条独立事务：中途失败不影响已提交条目的记录
  → 落快照 data/snapshots/{时间戳}-{指纹前8位}.json
```

指纹覆盖 `alpha_id` 与 `prediction_result` 两项：清单增删条目、或某条预判结论变化，指纹即变、确认即失效。快照内容：时间、指纹、清单全量、确认留痕、逐条提交结果。

**三重兜底**（dry-run 零调用 / 指纹绑定 / 快照落盘）全部可自动断言，对应 PRD #8 的五项测试点。

### 4.7 凭据处理（PRD #9）

```text
路径：$BRAIN_CREDENTIALS_PATH 或 <项目根>/.brain_credentials（L1）
格式：JSON 数组 [email, password]
缺失 / 格式错 / 认证失败 → 明确报错 + 退出码 2，不打印任何凭据内容
```

红线：凭据只入内存 `Credentials` 实例；不写回任何文件；不进日志；HTTP 层的 `Authorization` 头与 session cookie 不落盘。日志采用**白名单式**记录（只记方法名、alpha_id、状态码、耗时），而非「记录全部再脱敏」——后者一旦漏一处就泄露。

`#9` 的结构化扫描断言（测试不读凭据明文）由此可行：测试只需断言日志与库文件中不出现 `password` / `credentials` 字段名与 Basic 认证串特征。

---

## 5. 前端或交互影响

无 Web 界面（PRD 明确排除）。交互面是 CLI 与 `report` 的文本产物。

面向 Owner 的两条可用性约定：

- 长任务（`sync --full`、`precheck`）输出进度行：当前窗口 / 批次、已完成条数、预估剩余。相关性接口慢到分钟级，无进度输出会让人误判为卡死。
- `precheck` 启动前先打印候选总数与**按最坏单条耗时估算的总时长**，由 Owner 决定是否加 `--limit` 分次跑（见 §7 风险 R-1）。

---

## 6. 迁移和兼容性

### 6.1 v0.1 内部

首版无历史数据，无迁移。`meta.schema_version` 在启动时校验，不匹配则拒绝运行并提示——为 v0.2 的 schema 变更预留唯一必要的钩子。

库文件损坏或需重来：删除 `data/inventory.db` 重跑 `sync --full` + `classify`。代价是重新拉取，不丢失不可重算的信息——**除人工确认与提交结果外**（这两项由 `data/snapshots/` 的快照文件独立留档，这正是快照不入库的第二个理由）。

### 6.2 面向 v0.2 / v0.3 的接口边界

只在三处留边界，且都**不产生额外代码**：

| 边界 | 形态 | 为什么现在留 |
|------|------|-------------|
| 相关性数据来源 | `BrainClient.get_*_correlation` 的返回类型 `CorrelationResult` | v0.2 若改用本地 PnL 快算（`selfcorrQuick`），换实现不动 `precheck` 逻辑；现在无额外抽象层，只是返回类型稳定 |
| 分流规则版本 | `RULE_VERSION` 常量 + 库中 `classify_rule_version` 列 | 规则演进后旧结论仍可解释（A10 已要求，非新增） |
| 漏斗状态枚举 | 库中为 TEXT，非 CHECK 约束 | v0.3 加「未回测」态时不需 schema 迁移 |

**不做**的预留：不建 provider 注册表、不建插件机制、不为 Agent Hub 预留接口（PRD 明确「预留，不实现」）。

---

## 7. 风险和取舍

### 7.1 风险

| # | 风险 | 影响 | 处置 |
|---|------|------|------|
| R-1 | **预判耗时可能达数小时**。相关性接口最坏 100 秒/条；两阶段短路后，N 条候选的最坏总耗时约 N×100 秒（全部 self 即判死）到 N×200 秒（全部走完两阶段）。若可提交候选有 500 条，最坏 14~28 小时 | 高——直接决定 #6 在真实库存上是否跑得完 | 设计侧已尽力：两阶段短路减半、分批、断点续跑、`--limit` 分次。**但这是平台速度决定的下限，设计无法再压缩**。提请 Owner 知悉：先跑 `classify` + `report` 看候选规模，再决定预判范围（可先按 gap 排序只预判最优的 N 条）。若 Owner 认为需要收窄 v0.1 范围，走 Change Note |
| R-2 | **`count` 字段本身可能被封顶**（实测未提交恰为 10000） | 中——影响 §4.1 窗口级对账的左值可信度 | 叶子窗口的 `count < 9000`（低于疑似上限），不触及封顶；`count >= 9000` 的窗口一律细分。逻辑自洽。若某窗口细分到秒级仍 ≥ 阈值，`status=partial` 留痕 |
| R-3 | **提交前检查触发端点是否存在，仍未被证伪**（PRD 风险 2 要求设计阶段查证） | 中 | 见 §7.3 查证结论 |
| R-4 | **平台撤回能力未知**（PRD 风险 3） | 中 | 见 §7.3 查证结论 |
| R-5 | 平台并发上限（Demo 的 `ace_lib` 证实存在并发限制需退避） | 低 | v0.1 全串行，天然规避。`RateLimitError` 退避作为兜底 |

### 7.2 关键取舍

| 决策 | 选择 | 备选 | 为什么不选备选 |
|------|------|------|----------------|
| 存储 | SQLite | JSON 文件 / DuckDB / Postgres | JSON 文件无事务，断点续跑与并发写不安全；DuckDB 长于分析、短于事务写入；Postgres 引入服务端运维，违背「本地运行」定位 |
| 原始字段 | 只存 `raw_json` + 抽索引列 | 每字段建列 | 拆列造成同一数据两处存储，与「平台原始不可变、单一真源」冲突；解析成本在手动命令下可忽略 |
| 派生数据 | 独立表 | 同表加列 | 独立表让「同步不触碰派生」由 schema 保证，而非靠代码纪律；PRD 的字段分层是硬约束，值得用结构来兜 |
| 预判路径 | 平台相关性接口 + 两阶段短路 | 本地 PnL 快算（`selfcorrQuick`） | PRD #6 定稿口径写明「self/prod 相关性接口」；本地快算需先拉全部对照集 PnL，v0.1 引入新数据面与新算法，成本与风险都更高。已在 §6.2 留好换实现的边界，v0.2 再评估 |
| 预判断点 | 复用 `prediction_result` 非空 | 独立进度表 | 「已有结论就跳过」与「断点续跑」本是同一件事，两个状态载体必然漂移 |
| CLI 框架 | argparse | click / typer | 五条子命令，标准库够用，少一个依赖 |
| 数据校验 | dataclass | pydantic | 外部输入只有平台 JSON，校验点集中在客户端层 |

### 7.3 设计阶段查证结论（PRD 风险 2 / 风险 3）

**风险 2（提交前检查触发端点）——查证范围内未发现，结论维持 PRD 方案 B。**

已查证的证据面：`platform_functions.py` 全量 2600 行（快照）、`demo-code-deep-dive.md` 对 pip 包 v4.1.4（3792 行 / 51 工具）的比对结论、`brain-platform-facts.md` 的 API 实测记录。三者一致指向：Demo 的 `get_submission_check` **不是平台判定**，只是 `check_correlation` + `get_alpha_details` 的本地拼装，`all_passed` 只反映相关性（`demo-code-deep-dive.md` §3.5）。`DATA_DIVERSITY` / `REGULAR_SUBMISSION` / `POWER_POOL_CORRELATION` 三项在任何已知端点中都无获取路径。

**未能完成的部分**：真正证伪需要对平台发起探测请求（如 `OPTIONS /alphas/{id}`、试探 `/alphas/{id}/checks` 一类端点）。这需要真实账号凭据，属于对外部系统的实际调用——**我不在设计会话中自行执行**。建议：若 Owner 认为值得，授权一次只读探测（不含任何 POST），结论回填 PRD 风险 2；探明可得后按 PRD 既定路径经 Change Note 将 #6 从「预判」升级为「真判定」，本设计的 §6.2 边界已让该升级不必改动上层。

**风险 3（撤回能力）——同上，按不可回滚执行。** Demo 全代码中只有 `POST /alphas/{id}/submit`，无任何撤回 / 删除已提交 Alpha 的端点。设计一律按不可逆对待：dry-run + 指纹确认 + 快照落盘三重兜底不因后续查证结果而放松。

**风险 5（session 生命周期）——已关闭一半。** 认证自愈模式（§3.1.1）使 session 生命周期无需实测；限流阈值仍未知，但 v0.1 全串行 + `RateLimitError` 退避已将其降为低风险。

### 7.4 受保护路径

本设计新增的受保护路径（连同已有项）写入 `docs/knowledge/decisions/ADR-0003-受保护路径名单.md`，删除须走架构师 Review 门禁。要点：`alpha_platform/` 业务源码、`.brain_credentials`、`docs/research/AI打工人_demo/`、工作流框架文件；`data/` **不属**受保护路径（可重算，删除后重跑即可恢复）。

### 7.5 ADR 索引

| ADR | 决策 |
|-----|------|
| `ADR-0001-技术栈与存储选型.md` | Python + SQLite + 原始/派生双表分层 |
| `ADR-0002-全量同步切片与对账策略.md` | 时间窗自适应二分切片；窗口级对账为主口径；offset 穷尽降为诊断路径 |
| `ADR-0003-受保护路径名单.md` | 项目受保护路径定义 |
| `ADR-0004-提交前预判的实现路径.md` | 平台接口 + 两阶段短路；本地 PnL 快算列为 v0.2 备选 |

---

## 8. 实现阶段交接清单

供 Developer 会话直接取用：

1. **先建骨架再填逻辑**：`db.py` schema → `client.py`（含替身注入点）→ 其余模块。客户端层是所有自动化验收的前提，先立住。
2. **一键验证入口（DevOps N4）**：提供 `make test`（或 `./run-checks.sh`）单条命令覆盖全部 9 项自动验收，降级模式下 Owner 的阶段确认直接贴该命令输出。
3. **`.gitignore` 追加 `data/`**（DevOps N2），须在第一个产生运行产物的 commit 之前。
4. **测试替身**：`BrainClient` 的替身需覆盖——不同 limit 的 checks 样例（#5）、相关性录制回放样例（#6）、提交零调用探针（#8）、异常路径（#9）。
5. **不照搬 Demo 代码**：其凭据处理（明文回写）、错误处理（吞异常返回 False）、硬编码阈值均与本项目约束冲突。只借鉴机制模式（fingerprint 去重、断点、退避），不复制实现。

## Review 记录

### R1 — DevOps（运维/部署工程师） · 2026-08-15 · 结论：通过（附条件 N6/N7/N8）

**独立性**：本会话为 DevOps Review 会话，未参与设计文档与 ADR 的任何产出，不构成自审。

**审查范围**（按指定）：运行产物目录与 git 忽略（N2）、断点状态生命周期（N3）、一键验证入口（N4）、凭据加载与认证自愈、长任务韧性、不可逆动作的证据与一致性。不评数据模型合理性与工程成本（Developer 领域）。

**PRD 阶段 DevOps 意见的落地核对**

| 意见 | 落点 | 核对结论 |
|------|------|----------|
| N2 运行产物目录与 git 忽略 | §1.4 统一 `data/` 目录、整目录忽略；§8 交接清单第 3 条要求在首个产生产物的 commit 之前追加 `.gitignore` | **已处置**。把时点钉在「首个产生产物的 commit 之前」而非泛泛「实现阶段补」，是我提这条时想要但没写出来的精度。 |
| N3 断点状态生命周期 | §2.3 明确为临时派生数据：可随时清空、清空后果仅为重新切片、UPSERT 不产生重复、备份可不含本表 | **已处置**，四个问题（归属 / 可删性 / 删除后果 / 备份口径）全部回答。 |
| N4 一键验证入口 | §8 交接清单第 2 条 `make test` / `./run-checks.sh` 单命令覆盖，降级留痕直接贴输出 | **已处置**（数字有误，见 N9）。 |
| 凭据纪律 | §4.7 白名单式日志、§3.1.1 明确不采纳 Demo 的明文回写 | **方向正确**，但有一条未封堵的泄露路径，见 N6。 |
| 长任务韧性 | §3.1.1 认证自愈关闭 PRD 风险 5 的 session 半边；§3.1.2 退避参数全部可配 | **已处置且超出预期**。用「按需重认证」把「实测 session 生命周期」这个未知项直接消解掉，比测出一个会变的数字更稳。 |

R1 的 N2/N3/N4 全部销账。

**R1 新增意见**

| # | 问题 | 严重度 | 建议 |
|---|------|--------|------|
| N5 | **§7.4 与 ADR-0003 对 `data/` 保护范围的表述冲突**。§7.4 写「`data/` **不属**受保护路径（可重算，删除后重跑即可恢复）」，而 ADR-0003 明确 `data/snapshots/**` 受保护、是 `data/` 中唯一例外（「它是证据，不是产物」）。ADR 是名单真源且判断正确，但设计正文是实现会话的首要读物——按 §7.4 字面理解会把提交证据当作可重算产物。 | 中 | §7.4 补出例外：「`data/` 不属受保护路径，**但 `data/snapshots/` 除外**（不可重算的提交证据，见 ADR-0003）」。属文本对齐，不改决策。 |
| N6 | **`Credentials` 的 dataclass 默认 `__repr__` 会输出 password**。§3.1 定义 `@dataclass(frozen=True) class Credentials: email: str; password: str`，未关闭 password 的 repr。白名单式日志只管住了显式日志调用，管不住未捕获异常的 traceback——若某个持有 `Credentials` 的帧抛异常，`logging.exception`、`traceback.print_exc` 或调试工具打印局部变量时都可能把明文密码写进 stderr 或日志文件，直接违反 PRD #9 与 project-context 凭据硬约束。 | 中 | `password: str = field(repr=False)`（或自定义 `__repr__`）。零成本，建议直接写进 §3.1 契约——它是契约的一部分，不是实现细节，写在这里实现会话才不会漏。可顺带加一条断言：`"password" not in repr(credentials_instance)`。 |
| N7 | **日志落盘位置未定义**。§4.7 规定了日志内容口径（白名单式），§1.4 的运行产物布局却只有 `inventory.db` 与 `snapshots/`，没有日志；PRD #9 的验证方式是「日志与库存文件的结构化扫描断言」——测试要扫描日志，就得知道日志在哪。当前既不能确定日志是否落盘，也不能确定落在何处（若落项目根且未被 `data/` 的忽略规则覆盖，还会有误提交风险）。 | 中 | 二选一并写进 §1.4：**(a)** 日志落 `data/logs/`（随 `data/` 一并忽略，#9 扫描该目录）；**(b)** v0.1 只输出 stderr 不落盘，#9 的扫描对象相应明确为「捕获的 stderr + 库文件」。任选其一，但必须定下来，否则 #9 的自动验收在实现阶段没有确定的断言对象。 |
| N8 | **提交成功 → 回写失败的窗口未处置**。§4.6 的「每条独立事务」保证了中途失败不影响已完成条目，但没有覆盖「POST 已成功、写库前进程死亡（被杀 / 断电 / 磁盘满）」这一窗口：该条平台侧已提交，本地仍是「待确认」。重跑时它会再次进入清单并被再次 POST——提交是唯一不可逆动作，重复调用的平台行为未知（`brain-platform-facts.md` 无记录）。实际损害大概率被平台侧拒绝二次提交而挡住，但本地会把一次**成功**的提交记成「提交失败」，快照与库存双双失真，而快照正是这个动作的唯一证据。 | 中 | 低成本兜底二选一：**(a)** 提交前先写意向标记（`submit_result = 'in_flight'`）再 POST，重跑时遇 `in_flight` 不自动重提、转人工核对；**(b)** 维持现状但在 §4.6 显式记录该窗口，并规定重跑前先 `sync --incremental --stage OS` 用平台 `stage` 反查真实状态。我倾向 (a)：一次额外写库换掉一个不可逆动作的状态歧义，代价合适。 |
| N9 | **§8 交接清单第 2 条的「全部 9 项自动验收」数字与 PRD 不符**。PRD 定稿验收为 #1–#10 共 10 条，且 10 条**全部**含自动验证部分（#6/#7 为「自动 + 人工抽检」组合，#8 的真实提交由 Owner 抽验但其余五项测试点仍自动）。实现方按「9 项」构建验证入口，可能恰好漏掉一条而不自知——降级模式下没有 CI 兜底，这个入口就是唯一的门禁。 | 低 | 改为「覆盖 PRD #1–#10 全部验收标准的自动验证部分」，不写具体条数，避免与 PRD 后续变更脱节。 |
| N10 | **快照的异地备份责任已推给 Owner，但无任何运行时提醒**。ADR-0003 明确「`data/snapshots/` 若需长期保全，应由 Owner 自行做异地备份——本 ADR 不承诺技术强制」。这个边界划得对，但 `data/` 整目录被 git 忽略，意味着唯一不可重算的证据在本地只有单份、无版本控制、无备份，而 Owner 不会在阅读 ADR 的时刻想起这件事。 | 低 | `submit --confirm` 成功后在终端末尾输出一行：「快照已写入 `data/snapshots/xxx.json`（不入 git，建议异地备份）」。一行输出，把责任提醒放在唯一会触发它的时刻。 |

**确认到位、无需修改**（DevOps 视角）

- **§3.1.1 认证自愈**是本设计最漂亮的一处：把 PRD 风险 5 中「session 生命周期未实测」这个未知项，用「按需重认证」直接消解——不需要知道有效期是多久。同时明确不采纳 Demo 的明文回写、不引入 playwright 生物识别分支（遇 persona 直接抛 `AuthError` 交人工），两条都踩在项目硬约束上。
- **§3.1.3 异常契约**把「相关性重试耗尽」定为**不抛异常、返回 `None`**，与三类异常分开——长任务里这是对的：一条候选查不到不该炸掉整批。
- **退出码 4 与 5 的区分**（指纹失效 = 安全语义、需重新确认；中断可续 = 韧性语义、重跑即可）在降级模式下价值很实：Owner 不看日志就能分辨该重跑还是该复核。
- **§4.6 三重兜底**（dry-run 零调用 / 指纹绑定 `alpha_id:prediction_result` / 快照落盘）完整承接 PRD #8，指纹覆盖预判结论这一点比只覆盖 ID 更严。
- **ADR-0003 的判据选得好**：受保护 = 「不可重算」而非「重要」，并显式论证了「保护整个 `data/` 会让门禁变橡皮图章」。`data/inventory.db` 明确排除，使「删库重来」成为无摩擦操作——这正是 M4 想要的形态。
- **§7.3 拒绝在设计会话中自行发起平台探测**（真正证伪需真实凭据的对外调用，转请 Owner 授权）符合 A4 对外动作的边界，处理得克制。
- 范围仍无部署动作 → **部署就绪检查阶段预期为「已跳过（本迭代无部署，本地运行）」**。

**放行条件**：DevOps 侧无阻塞项、无高严重度缺陷。N6/N7/N8 建议在定稿前补入设计正文（三处都是契约级、成本很低，写在这里实现会话才读得到；若判定延期，请显式记录延期结论）；N5/N9 为文本对齐；N10 为可选建议，不处置也不影响定稿。

> 备注：本设计「Review 状态」表 DevOps 行与 `v0.1.md` 设计阶段门禁行的 Review 结果归 Architect 维护，Review 方不改正文，请 Architect 定稿时更新。
