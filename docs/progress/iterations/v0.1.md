# v0.1 迭代记录

## 概览
- 目标：打通「库存同步 → 规则分流 → 提交候选确认」最短闭环——把 1 万+ 未提交 Alpha 的漏斗状态管起来，产出可提交清单
- 开始时间：2026-08-13
- 关闭时间：
- 最终状态：进行中
- 类型：标准迭代
- 当前阶段：实现
- 收尾状态：未收尾

## 阶段门禁

> Bootstrap 初始化、迭代关闭确认和收尾归档不是迭代内的阶段，而是独立的非角色机制。触发时机和执行规则见 `docs/baseline/mechanisms.md`。
> 本区只放标准阶段标题：`PRD 阶段`、`设计阶段`、`实现阶段`、`部署就绪检查`。讨论记录、未来方向、停放性事项不得作为本区三级标题；应放入 `Review 记录`、`Change Notes` 或独立 ad-hoc。
> 阶段门禁状态只允许：`待Review / Review中 / 修改中 / 已定稿 / 阻塞 / 已跳过`。部署就绪检查使用独立状态：`待检查 / 检查中 / 通过 / 阻塞 / 已跳过`。门禁表最新轮次置于末行。

### PRD 阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | PM 初版；Owner 已确认范围（2026-08-15），送审 | Architect、Developer（另开会话冷启动）、DevOps（Owner 于 2026-08-15 追加） | Developer：需修改（D1 高 / D2·D3 中 / D4 低 + Q1-Q4 待澄清，2026-08-15）；DevOps：通过（附条件）（H1 高 / M1-M4 中 / L1 低，2026-08-15）；Architect：不通过（A1 阻塞 / A2-A5 高 / A6-A8 中，2026-08-15） | Review中 |
| R2 | PM 修订（2026-08-15）：A1 选方案 B（#6 改「提交前预判」）、新增 §3 状态机与分流规则、验收 #1-#10 全面改写、H1/M2-M4/L1 落验收与设计约束、M1 转 Owner 拍板；修订对照见 PRD | Architect、Developer（复核）；DevOps（Owner 指定复核，2026-08-15） | Developer：通过（附条件）（R1 的 D1-D4·Q1-Q4 全部核销；新增 D5·D6 中 / D7·D8 低，均不阻塞定稿，2026-08-15）；DevOps：通过（R1 的 H1·M2-M4·L1 全部处置，M1 经 Owner 裁决核销，附条件解除；新增 N1 中 / N2-N4 低，均不阻塞定稿，2026-08-15）；Architect：通过（附条件）（R1 的 A1-A8 全部核销、阻塞解除；新增 A9·A10 中 / A11-A13 低，均不阻塞定稿，2026-08-15） | Review中 |
| R2 定稿 | PM 定稿前处置附条件 9 项（D5-D8 / A9-A13 / N1 补入正文，见 PRD「R2 附条件处置记录」；N2-N4 与 Architect §四清单结转设计阶段），PRD 定稿（2026-08-15） | —（三方已全通过，无新轮次） | — | 已定稿 |

### 设计阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | Architect 初版（2026-08-15）：`v0.1-design.md` + ADR-0001~0004；技术栈 Python+SQLite、双表字段分层、时间窗自适应切片与窗口级对账、预判两阶段短路；DevOps N2/N3/N4 与 Architect §四承接清单已落实；PRD 风险 2/3 查证结论入 §7.3 | Developer、DevOps（另开会话冷启动） | DevOps：通过（附条件）（N2-N4 全部销账；新增 N5·N9 文本对齐 / N6·N7·N8 中——凭据 repr 泄露、日志落盘位置未定、提交回写窗口，均不阻塞定稿，2026-08-15）；Developer：需修改（D9·D10 高——同步丢首末记录且对账查不出、`alpha_state` 初始态缺失叠加 NOT IN 的 NULL 陷阱；D11-D15 中；Q5·Q6 待澄清，2026-08-15） | Review中 |
| R2 | Architect 修订（2026-08-15）：两方 15 项意见逐条验证后**全部采纳、无驳回**——D9 根窗口外扩 1s + 全局总量交叉验证、D10 同事务建初始态（只创建不覆盖）+ NOT NULL 消除 NULL 陷阱 + 守恒断言、D11 递归收敛三约束、D13 依赖注入契约（§3.1.0 新增）、D14 预判 fail 保持改造候选、D15 `--retry-pending` 与 `report --status`、Q5 `status_source` 列、Q6 意向快照先落、N6 `field(repr=False)`、N7 日志落 `data/logs/`、N8 `in_flight` 意向标记、N5/N9/N10 文本与提示；ADR-0001/0002 同步修订；D12 口径变更走 Change Note `CN-001`（待 PM 确认） | Developer、DevOps（复核） | DevOps：通过（R1 的 N5-N10 六条全部落实，附条件 N6/N7/N8 解除；新增 N11 中——`in_flight` 无 CLI 出口 / N12·N13 低，均不阻塞定稿，2026-08-15）；Developer：需修改（R1 的 D9-D15·Q5/Q6 全部核销；新增 D16 高——D14 的修法踩了 D10 同型 NULL 陷阱且方向相反，致「从未预判的改造候选」被静默排除出重算、验收 #5 对改造候选整体失效，已实机验证；D17 中——§4.3 正文与 SQL 自相矛盾；D18 中、D19 中（同 DevOps N11）、D20 低，2026-08-15） | Review中 |
| R3 | Architect 修订（2026-08-15）：两方 8 项意见（D16-D20 / N11-N13，D19≡N11 合并）逐条验证后**全部采纳、无驳回**——D16 把 `fail` 判断移出 SQL 到写回逻辑（不补 `ifnull`，从机制上消除整类 NULL 陷阱，已实机验证）并一并解决 D17、D18 补括号与保护态守卫、D19+N11 新增 `submit --reconcile` 与退出码 6、D20 守恒断言补「已回测」恒成立、N12 快照写盘失败即终止、N13 验证入口标注口径依据。**另按 Owner 指示先完成 Demo 全量代码一手核验**（`ad-hoc/2026-08-15-spike-demo-code-firsthand-audit.md`，通读 3401 行）：R-1 耗时订正 100s→80s、分页 `limit` 可配至 500、§8 补「不返回 Response 对象」、§7.3 证据主体拆表订正、ADR-0004 补证据强度说明 | Developer、DevOps（复核） | Developer：需修改（R2 的 D16-D20 全部核销；新增 D21 高——`submit --reconcile` 用增量同步取平台 `stage`，而增量按 `dateCreated` 只拉新增、拉不到已入库记录，收敛判据取到旧值会把「已提交成功」误判为未提交并再次 POST，且 `BrainClient` 无按 id 查单条的方法致该命令当前无法实现；D22/D23 低、Q7 待澄清，2026-08-15）；DevOps：需修改（R2 的 N11-N13 全部落实；新增 **N14 高**——独立核出，与 D21 同源，补三点 DevOps 定性：失效方向为危险失效而非保守失效、属「亮绿灯但数据错」第六条须进 §8 自测清单、`get_alpha` 直查使收敛判据绕开本地库与水位逻辑；N15 低——reconcile 遇平台查询失败的处置未定义，2026-08-15）<br>**流程提示**：两方均「需修改」，按速查 §9-10 已触发「R3 未通过升级阻塞交 Owner 决策，不进 R4」；两方一致判断缺陷单点、修法明确，建议 Owner 裁决走「修入正文 → 快速确认 → 定稿」，是否进 R4 由 Owner 决定 | Review中 |
| R4 | Architect 定向修订（2026-08-15，**Owner 裁决走快速通道**，留痕见异常升级记录）：R3 两方 6 项意见（D21-D23 + Q7 / N14-N15，N14≡D21）逐条验证后**全部采纳、无驳回**——D21≡N14 补 `BrainClient.get_alpha(alpha_id)`（`GET /alphas/{id}`）+ `--reconcile` 改逐条直查、删掉「先增量同步」这一步；N15 查询失败保持 `in_flight` 不动（不确定就不动）；§8 第 6 条扩为六条并附 N14 指定用例；D22 §4.4 数字同步 80 秒；D23 `--reset` 守卫收窄到 `status_source=prediction`；Q7 `is_pending_flag` 归预判侧 + §4.5 统计口径限定。**未改动 R3 意见范围之外的内容** | Developer、DevOps（快速复核：只核 R3 意见落点，不重审全文） | DevOps：通过（N14/N15 均已落实——`get_alpha` 直查 + 删掉增量同步那步 + §8 第 6 条第六项用例一字不差采纳；N15 还补齐了「全部收敛则退出码 0」；另复核修法本身的异常路径与快照批次归属，均闭合无危险失效分支。新增 N16 低——§3.1.0 替身方法枚举未同步 `get_alpha`，一行文本，处置后可直接定稿，2026-08-15）；Developer：**通过**（D21≡N14 取数路径改逐条 `get_alpha` 直查、N15/D22/D23/Q7 与 §8 第六条用例全部落地，无改动溢出；新增 D24 低——reconcile 未同步 `alpha` 表 `stage`，无功能影响，2026-08-15） | Review中 |
| R4 定稿 | Architect 定稿前处置两条低（N16 §3.1.0 替身枚举补 `get_alpha` + §8 第 4 条补 reconcile 用例；D24 收敛时同事务 UPSERT `alpha` 表），见设计文档「R4 定稿前处置记录」；ADR-0001~0004 同步转「已采纳」。**Owner 确认定稿（2026-08-15）** | —（两方 R4 已全通过，无新轮次） | — | **已定稿** |

### 实现阶段
| 轮次 | 产出方动作 | base_commit | head_commit | 指定 Review 方 | Review 结果 | 验证 | 阶段状态 |
|------|------------|-------------|-------------|------------------|-------------|------|----------|
| R1 | Developer 按设计 §8 交接清单实现全部模块（2026-08-16）：db schema → BrainClient → classify / sync / precheck / report / submit / cli + config / credentials；TDD 逐模块推进，自测报告见 `v0.1-test-report.md`。**含 1 处对设计字面的偏离待 Architect 裁定**（§3.1.1 认证探活改为「首次认证 + 401 自愈」，理由与覆盖见自测报告「需 Review 方裁定的偏离」） | fda71fb | 4d0730f | Architect（契约一致性 + 偏离裁定 + 验收独立复核）、DevOps（运行产物 / 凭据纪律 / 验证入口） | Architect：需修改（**A14 高**——`submit --reconcile` 不补齐快照、`snapshot_dir` 参数未使用，设计 §4.6 明确要求且断了不可逆动作的证据链；**A15 高**——`count` 缺失默认成 0 致整窗静默丢数而对账仍判绿，已实机验证；A16-A20 中：分批未实现 / 步骤 5 全局对账缺失 / 相关性只取 schema.max / `--reset` 语义偏离 / classify 无事务；A21-A24 低。**2 处偏离均裁定接受**：① 认证探活改 401 自愈 ② `--reset` 与常规候选集互斥而非 `OR`（实现更安全，设计的 OR 会让 --reset 意外触发全量预判）——按速查 §11 属「实现取舍」，**设计不改**，裁定见自测报告 Review 记录，后续勿按设计字面改回；2026-08-16）；DevOps：需修改（**O3 高，同 A14**——`reconcile` 的 `snapshot_dir` 仅在签名、函数体零次使用，恢复路径跑完证据仍停在意向态，库中状态与快照长期不一致，而两个载体本是为互印设计；**此条本方首轮漏检、由 Architect 抓到**，补两点验收要求：每条收敛后即补齐、加「状态与快照一致」交叉断言。**O1 放行前置**：三条实现 commit 未推送，`l1-gates` 从未在实现代码上跑过，按 [P0] fail-closed「查不到结论」等同非绿灯、不得推进；已用 `git archive` 干净检出预演 CI 环境，lint 退出 0、93 passed，预计 push 后绿灯。**O2 中**——`Makefile` 的 `PY := .venv/bin/python` 硬编码致 CI 无法复用 `make check`，且两者 lint 排除规则不同（已实证含 `.venv` 环境下 CI 口径退出 1），自测报告「与 l1-gates 同口径」表述不准确；CI 那半边由 DevOps 承接。产物落位 / 凭据纪律 / `confirm` 侧证据链 / 六条路径覆盖均已到位，2026-08-16） | pytest 93 passed；compileall 通过；`l1-gates` 结论以 push 后当前 commit 为准 | Review中 |

### 部署就绪检查
| 状态 | 目标环境 | 证据 | 阻塞项 |
|------|----------|------|--------|

## 阶段执行记录

> **留痕对偶（BCR-015 L1）**：每阶段完成时追加一行——动作摘要、门禁项与结果、进入下一阶段的依据。**降级模式下**（约定名门禁 `l1-gates` 非绿灯结论）每阶段追加一行：「本阶段运行于降级模式：`l1-gates` {未通过 / 不存在 / 结论不可得}，推进依据 = Owner 确认（非门禁）」。本节与下两节是 **Owner 抽检入口**；角色日志是详情层，不作为抽检入口。

| 阶段 | 动作摘要 | 门禁项与结果 | 进入下一阶段依据 |
|------|----------|--------------|------------------|
| PRD 阶段 | R1 三方 Review（Architect 不通过 / Developer 需修改 / DevOps 通过附条件）→ PM R2 修订处置 18 项 → R2 三方通过 → 定稿前处置附条件 9 项 → 已定稿 | 本阶段运行于降级模式：`l1-gates` 不存在（Owner 已裁决接受，见异常升级记录），推进依据 = Owner 确认（非门禁） | **Owner 已确认进入设计阶段**（2026-08-15，PM 会话原话「确认进入设计阶段」） |
| 设计阶段 | Architect 出 R1 → 两方 Review 四轮（R1 需修改 / R2 需修改 / R3 两方均需修改并触发升级 / R4 两方通过）→ 中途按 Owner 指示插入 Demo 全量代码一手核验（3401 行）→ 定稿前处置 N16/D24 → 已定稿。累计 31 条编号意见 / 29 个独立问题，全部采纳无一驳回；四份 ADR 转「已采纳」 | 本阶段运行于降级模式：`l1-gates` 不存在（Owner 已裁决接受，见异常升级记录第 1 行），推进依据 = Owner 确认（非门禁）。另：R3 两方均未通过触发速查 §9-10 升级阻塞，经 Owner 裁决走快速通道（异常升级记录第 2 行） | **Owner 已确认定稿**（2026-08-15，本会话原话「定稿」）；据此进入实现阶段 |

## 验收证据链

> 自测报告「验收证据链」表的镜像索引（留指针即可，不复制全表）；迭代关闭检查据此核对每条验收标准有对应结果。

- 自测报告证据链：`v0.1-test-report.md` §验收证据链（已创建，2026-08-16）
- 全绿 / 含人工抽检项数：自动部分 10/10 全绿（93 项用例）；人工抽检 3 项——#6 真实小批量执行、#7 报告可读性、#8 真实提交，均按 PRD 既定口径留待 Owner 验收时抽验

## 异常升级记录

> 何时、何因升级 Owner，裁决结果。无升级时留空表。

| 时间 | 触发阶段 | 升级原因 | Owner 裁决 |
|------|----------|----------|------------|
| 2026-08-15 | PRD 阶段（R1 Review 发现，DevOps M1） | 项目无 CI，约定名门禁 `l1-gates` 不存在，属非绿灯——须 Owner 显式选择建最小 CI 或接受降级模式 | **接受 v0.1 全程降级模式**：不建 CI；每阶段推进由 Owner 确认，并在「阶段执行记录」逐阶段写降级留痕（**已被 2026-08-16 二次裁决覆盖，见下行**） |
| 2026-08-16 | 设计定稿后 / 实现阶段开工前（跨任务待办 P1 到期） | DevOps M1 方案 (a) 时机窗口到期：代码与测试即将产生，是建最小 CI 的最后合理时点，须 Owner 显式再裁一次 | **建最小 `l1-gates`**（PM 会话，Owner 采纳 PM 建议）：由 DevOps 在 Developer 开工前建 GitHub Actions 工作流，只跑单测 + lint、不接触真实平台；建成后实现 / 部署阶段推进恢复「`l1-gates` 绿灯 = 自动推进」正常模式，PRD / 设计两阶段的降级留痕保持有效不追溯 |
| 2026-08-15 | 设计阶段（R3 两方均「需修改」） | `standard-iteration-quick.md` §9–10 规定「R3 仍未通过升级`阻塞`交用户决策，不进 R4」，本轮 Developer（D21 高）与 DevOps（N14 高，与 D21 同源独立核出）均未通过，按字面已触发。两方判断一致：缺陷单点（`--reconcile` 取数路径）、修法明确（补 `get_alpha` 直查）、无设计决策空间，均声明可快速复核放行；DevOps 明示「是否进 R4 由 Owner 决定，Review 方不自行放行」 | **走快速通道**：Architect 修入正文出 R4 → 两方快速确认（只复核 R3 意见落点，不重审全文）→ 定稿。不按阻塞流程重启设计阶段。（Owner 原话「按照你的建议修正」，即采纳 Architect 在会话中提出并经两方一致支持的处置建议） |

## Review 记录

## Change Notes

| Change Note | 状态 | 变更级别 | 受影响角色 | 执行状态 | 归档状态 |
|-------------|------|----------|------------|----------|----------|
| `CN-001-验收1主口径改为窗口级对账.md` | 已确认 | 轻量变更 | PM（已确认 2026-08-16）、Developer（已认同，即该意见提出方） | 已执行（PM 落 PRD 验收 #1 正文 + 定稿后变更记录，2026-08-16） | 未归档（迭代关闭时归档） |

## Git 关键节点

| 节点 | commit | 说明 |
|------|--------|------|
| 迭代启动 | （本次提交） | Brief 升级为 v0.1，PRD R1 初稿 |

## 迭代关闭归档
- Owner 验收（真源记录，未验收 / 打回则不得关闭）：未验收
- Developer 自测结论（证据齐备）：
- 关闭结论：
- 未关闭原因或延期项：
- 已生成 summary：
- 已更新 `docs/progress/INDEX.md`：
- 已更新当前角色日志：
- 其他角色待补充/确认：
- 已更新知识库：
- 遗留问题下一步：
