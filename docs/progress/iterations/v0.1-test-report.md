# v0.1 Developer 自测报告

> 测试阶段已并入实现：Developer 实现完成即做自测（接口/自动化），**证据必留**。手动点击验收由 Owner 在迭代关闭前完成（见迭代记录「Owner 验收」）。

## 文档状态
- 整体状态：待Review
- Review 轮次：R1
- 关联迭代：v0.1（实现阶段 R1）

## Review 计划
- Review 模式：动态 Review
- 影响领域：实现与设计契约的一致性（架构）、验收标准 / 边界 / 回归的独立复核（架构或运维）、运行产物与凭据纪律（运维）
- 本轮指定 Review 方：
  - Architect（架构师）——实现是否忠实于设计 §2/§3/§4 契约；**一处对设计字面的偏离需要裁定**（见下方「需 Review 方裁定的偏离」）；验收标准 / 边界 / 回归的独立复核
  - DevOps（运维/部署工程师）——运行产物落位（`data/` 三件套）、凭据纪律的结构化断言、一键验证入口与 `l1-gates` 的衔接
- 未指定角色及原因：
  - PM——本轮未改动任何产品口径与验收标准；`CN-001` 已由 PM 确认落入 PRD 正文，实现按其口径执行
- 少于 2 个 Review 方时的用户确认：无（已指定 2 方）
- 定稿条件：本轮指定 Review 方全部通过，且无未处理高严重度阻塞问题

## Review 状态
| Review 方 | 指定原因 | 状态 | 轮次 |
|-----------|----------|------|------|
| Architect | 设计契约一致性 + 偏离裁定 + 验收独立复核 | **需修改**（A14/A15 高；A16-A20 中；A21-A24 低；2 处偏离均裁定接受） | R1 |
| DevOps | 运行产物、凭据纪律、验证入口 | 待反馈 | R1 |

## 结论
- 自测判定：**通过**（93 项自动用例全绿；#6/#7/#8 的真实平台部分按 PRD 既定口径留待 Owner 抽验）

## 需 Review 方裁定的偏离（1 处）

设计 §3.1.1 写「每个公开方法进入时先 `_ensure_authenticated()`：检查 cookie → **GET `/authentication` 探活** → 失效则重认证」。

实现改为：**首次认证 + 业务请求遇 401 时自动重认证并重试一次**（`client.py::_ensure_authenticated` / `_request_json`）。

- 理由：按字面每次探活，会让全量同步的请求数**翻倍**（1 万条按 page_size=100 约 100+ 次业务请求 → 再加 100+ 次探活）；而 §3.1.1 自陈的目标是「无需实测 session 生命周期，**按需**重认证即可覆盖长任务中途失效」——401 触发正是「按需」，目标完全达成。
- 覆盖：`test_client.py::test_业务请求遇_401_自动重认证并重试一次` 断言认证被调用两次（首次 + 401 后）。
- 若 Architect 认为应严格照字面实现，改动量很小（在 `_ensure_authenticated` 内加一次探活请求），请在 Review 中明示。

## 自测范围（四维）

- **正常路径**：全量同步（切片 → 穷尽 → 对账）、增量同步、分流三分类、两阶段预判三态、报告四块、dry-run → confirm → 快照落盘、reconcile 收敛；CLI 五条命令串联跑通。
- **边界值**：时间轴首末条与同一秒多条（D9）；同一秒挤满记录导致无法细分（D11，落 `partial`）；`limit == 0` 与 `value/limit` 缺省的不可量化 FAIL；空清单指纹；`--limit` 截断；offset 封顶（切片能拉全、直连撞上限）。
- **异常路径**：认证失败 / 生物识别分支 / 凭据文件缺失与格式错；429 退避后成功与重试耗尽；5xx；相关性重试耗尽返回 `None`；预判中途抛异常（已完成部分不丢）；提交中途进程死亡（留 `in_flight`）；快照写盘失败（不进提交循环）；reconcile 查询失败（保持不动）。
- **状态转换**：已回测 → 三类候选；可提交候选 → 待确认 / 改造候选 / 待定标注；待确认 → 已提交 / 提交失败；`in_flight` → 已提交 / 退回待确认；保护态在分流与预判中均不被改写；同步再次执行时派生字段保持不变。

## 验收证据链

> 逐条对应 PRD 验收标准。执行入口统一为 `make test`（= `.venv/bin/python -m pytest -q`）。
> **验收 #1 的口径依据：`CN-001`（已由 PM 确认落入 PRD 正文）/ 设计 §4.1 步骤 4「窗口级对账」。**

| PRD 验收标准 # | 验证入口 | 执行结果 | 证据链接 |
|----------------|----------|----------|----------|
| #1 全量同步与双路对账 | `tests/test_sync.py::test_窗口级对账为主口径且逐窗口相等`、`::test_时间轴首末两端的记录不丢`、`::test_offset_封顶被检出并留痕` | 绿 | 逐窗口 `fetched == reported`；offset 封顶写入 `meta.offset_ceiling_detected` |
| #2 增量只拉新增且幂等 | `tests/test_sync.py::test_增量同步只拉新增且幂等` | 绿 | 第二次增量 `fetched == 0`，行数不变 |
| #3 字段完整 + 按状态查询 + 分层保留 | `tests/test_sync.py::test_再次同步刷新平台字段但派生字段保持不变`、`tests/test_report.py::test_按状态查询输出记录清单` | 绿 | 平台字段被刷新、派生字段逐项保持；`report --status` 输出清单 |
| #4 全量分流无残留 + 理由可查 | `tests/test_classify.py::test_全量覆盖无未分类残留且守恒断言成立` | 绿 | 三类归属正确、`classify_reason_json` 无空值、守恒断言通过 |
| #5 达标线动态、门槛变则结果变 | `tests/test_classify.py::test_达标线随平台返回值变化而非硬编码`、`::test_从未预判的改造候选在改阈值后归属随之变化` | 绿 | 注入不同 limit 断言归属变化；D16 针对性用例 |
| #6 提交前预判（自动部分） | `tests/test_precheck.py`（12 项：三态出口、短路、跳过已有结论、`--retry-pending`、阈值来源、`--limit`、中断不丢） | 绿 | 阈值带 `{"value":0.7,"source":"convention_demo"}` 写入每条结论 |
| #6 真实小批量执行 | — | **人工抽检** | 消耗平台配额且结果不可重复，按 PRD 口径由 Owner 验收时执行 |
| #7 漏斗报告（自动部分） | `tests/test_report.py`（7 项：七态计数、来源拆行、TOP 榜、A12、Q7、清单、两种格式） | 绿 | 各级数字与库存一致 |
| #7 报告可读性 | — | **人工抽检** | 可读性无法自动断言 |
| #8 未确认零调用 + dry-run + 指纹 + 快照 | `tests/test_submit.py`（12 项）、`tests/test_cli.py::test_dry_run_零调用并打印指纹`、`::test_指纹不匹配返回退出码_4`、`::test_存在_in_flight_时返回退出码_6` | 绿 | 零调用、指纹失效、意向快照先落、结果补齐、`in_flight` 拦截与收敛 |
| #8 真实提交 | — | **人工抽检** | 对外不可逆动作，由 Owner 在验收时抽验 |
| #9 凭据纪律 | `tests/test_cli.py::test_日志与库文件不出现凭据字段名或认证串特征`、`::test_凭据缺失返回退出码_2`、`tests/test_client.py::test_凭据的_repr_不含密码` | 绿 | 按字段名与 Basic 串特征扫产物；测试自身不读取凭据文件 |
| #10 同步中断可续跑 | `tests/test_sync.py::test_断点续跑跳过已完成窗口` | 绿 | 第二轮请求数显著少于首轮，行数不变 |

### §8 第 6 条「六条亮绿灯但数据错」的针对性覆盖

| # | 路径 | 用例 | 结果 |
|---|------|------|------|
| 1 | 全局时间边界丢首末记录（D9） | `test_sync.py::test_时间轴首末两端的记录不丢`（首条同秒两条 + 末条） | 绿 |
| 2 | `alpha_state` 缺行致 `NULL NOT IN` 静默排除（D10） | `test_db.py::test_funnel_status_非空由_schema_强制`、`test_classify.py::test_全量覆盖无未分类残留且守恒断言成立` | 绿 |
| 3 | 递归切片窄窗口不收敛（D11） | `test_sync.py::test_递归在窄窗口收敛而非无限下探`（12 条挤在同一秒） | 绿 |
| 4 | 预判 fail 记录被重算回可提交候选后卡死（D14） | `test_classify.py::test_预判判死的记录锁定状态但理由照常刷新` | 绿 |
| 5 | 适用域在 SQL 判断可空列（D16） | `test_classify.py::test_从未预判的改造候选在改阈值后归属随之变化` | 绿 |
| 6 | reconcile 经常规链路取状态（D21/N14） | `test_submit.py::test_reconcile_按平台真实状态收敛为已提交`——替身在 `list_alphas` 中**不返回**该 alpha、`get_alpha` 返回 `stage='OS'`，断言收敛为 `success` | 绿 |

## 结果摘要

| 类型 | 数量 |
|------|------|
| 通过 | 93 |
| 失败 | 0 |
| 阻塞 | 0 |

按模块：client 17、classify 13、precheck 12、submit 12、cli 9、sync 9、credentials+config 8、report 7、db 6。

## 缺陷列表

| 编号 | 严重程度 | 描述 | 状态 | 责任角色 |
|------|----------|------|------|----------|
| — | — | 自测过程中未发现未解决缺陷 | — | — |

自测过程中修正 1 处：日志用 `logging.basicConfig` 在 root 已有 handler 时静默失效，导致 `data/logs/` 不产生文件（N7 要求 #9 有确定的扫描对象）；改为只给 `alpha_platform` 包 logger 挂 `FileHandler`，不碰 root。已由 `test_cli.py::test_日志落在_data_logs_下` 覆盖。

## 回归结论

全量 93 项每次改动后整体重跑，无回归。`compileall` 语法检查通过（与 `l1-gates` 的 lint 步骤同口径）。运行环境为 Python 3.11 venv，与 CI 的 `python-version: '3.11'` 对齐——本机系统 Python 为 3.9.6，不满足设计 §1.2 的 3.11+ 要求，故用 `uv` 建隔离环境，`Makefile` 中的 `PY := .venv/bin/python` 即指向它。

全部用例在客户端方法层或传输层构造替身，**零真实平台请求**，不消耗账号配额、不产生任何不可逆动作。

## Review 记录

### R1 — DevOps（运维/部署工程师） · 2026-08-16 · 结论：需修改（O3 高，同 Architect A14）

**独立性**：本会话未参与任何实现产出，不构成自审。

**审查范围**（按指定）：运行产物落位、凭据纪律、一键验证入口与 `l1-gates` 的衔接、不可逆动作的恢复路径。不评数据模型与算法正确性（Architect 领域）。

**历次 DevOps 意见的实现落地核对**（逐条验证代码，不采信报告声明）

| 意见 | 落点 | 核对结论 |
|------|------|----------|
| N2 运行产物统一目录 + git 忽略 | `.gitignore` 含 `data/`；`config.py` 三件套默认 `data/inventory.db` / `data/snapshots` / `data/logs` | **已落实**，且 `.gitignore` 在首个产生产物的 commit 之前就位。 |
| N4 一键验证入口 | `Makefile` 的 `test` / `lint` / `check` | **已落实**（口径问题见 O2）。 |
| N6 `Credentials` repr 泄露 | `client.py:27` `password: str = field(repr=False)` + 注释保留泄露路径说明；`test_client.py::test_凭据的_repr_不含密码` | **已落实**，契约与断言都在。 |
| N7 日志落盘位置 | `cli.py::_setup_logging` 落 `data/logs/`；#9 扫描对象确定 | **已落实**。自测中还修掉一个真坑：`logging.basicConfig` 在 root 已有 handler 时静默失效、`data/logs/` 不产生文件——那会让 #9 的扫描断言对着一个不存在的文件「通过」。改为只给本包 logger 挂 handler、不碰 root，是正确解法。 |
| N8 / N11 / N14 / N15 不可逆动作链 | `submit.py`：⓪ `in_flight` 前置检查 → ① 意向快照先落 → ② a 标记落库 / b POST / c 回写 → ③ 补齐快照；`reconcile` 用 `client.get_alpha` 直查、查询失败保持 `in_flight` 不动 | **`confirm` 侧全部落实；`reconcile` 侧缺补齐快照——见 O3**。N14/N15 的取数路径与「不确定就不动」均已落到代码；但恢复路径没有闭合证据链那一步。**该缺陷由 Architect A14 抓到，我首轮通读漏检**（见 O3 说明）。 |
| N13 CN-001 过渡期口径 | `Makefile` 的 `test` 目标打印口径提示；自测报告证据链同步标注 | **已落实**（CN-001 已由 PM 落入 PRD 正文，该兜底现已成冗余保险，无害）。 |

**我实际执行的验证**（不只读代码）

| 验证 | 命令 | 结果 |
|------|------|------|
| 一键入口可用性 | `make test` | 93 passed，与报告数字一致 |
| lint 目标 | `make lint` | 退出 0 |
| **CI 环境预演** | `git archive HEAD` 导出干净检出（等价 `actions/checkout`），用 Python 3.11 跑 `compileall -q -x 'docs/research' .` 与 `pytest -q` | lint 退出 0、**93 passed** → push 后 `l1-gates` 应当绿灯 |
| 测试零真实请求 | 检索 `tests/` 全部平台 URL 与 `requests` 调用 | 无命中；全部经 `conftest.py::FakePlatform` 客户端方法层替身。CI 不会触达真实平台 |
| 事务落盘（`in_flight` 是否真生效） | 查 `db.py:93` 连接参数 | `isolation_level=None`（autocommit），每条 `execute` 立即落盘——`in_flight` 在进程死亡时不会随未提交事务回滚，N8 的意向标记**真实有效**。`sync.py` 另用显式 `_transaction`（BEGIN/COMMIT/ROLLBACK）满足 D10 的同事务要求 |

> 事务这条我原本按「设计 §4.6 明确写了『提交事务』、代码里却没有 `conn.commit()`」准备报缺陷，查过连接参数后确认是 autocommit、意向标记有效，遂撤回。记在这里是因为：**autocommit 是个容易被后续改动破坏的隐式前提**——若日后有人给 `connect()` 加上 `isolation_level`，`submit.py` 会在没有任何报错的情况下丧失崩溃安全性。建议实现阶段收尾时在 `db.connect()` 处补一行注释说明这个依赖关系（一行，非必修）。

**意见**

| # | 问题 | 严重度 | 建议 |
|---|------|--------|------|
| O1 | **`l1-gates` 从未在实现代码上运行过——这是放行前置，不是代码缺陷**。三条实现 commit（`9ec10bf` / `4d0730f` / `3713df1`）均**未推送**，远端 HEAD 仍停在 `fda71fb`（CI 建成那条）。按 `runtime.md` [P0]（fail-closed）：「绿灯以外——红 / 未跑 / 进行中 / **查不到** / 无该 workflow——一律不自动推进」。当前门禁结论**不可得**，因此实现阶段不具备推进条件。这与 Developer 的产出质量无关（push 归 Owner 决定），但门禁状态是我的判断范围，必须显式标出。 | 阻塞（流程门禁） | `git push` 后确认 Actions 绿灯即解除。**我已预演过 CI 环境**（见上表）：干净检出下 lint 与 93 项测试全绿，预计不会红。这也是 `l1-gates` 建成后第一次真实门禁应用，值得确认它确实按预期放行。 |
| O3 | **`reconcile` 未补齐快照——确认 Architect A14 成立，并补 DevOps 定性；此条我首轮漏检**。已验证：`snapshot_dir` 仅出现在 `reconcile` 的签名里，函数体**零次使用**，`cli.py:157` 传了参数却被丢弃；`_patch_snapshot` 只在 `confirm` 末尾被调用。设计 §4.6 明确要求 reconcile「原地补齐对应快照的逐条结果（**补上中途死亡时缺失的步骤 ③**）」。DevOps 角度的定性：这是**双重失败**——快照是唯一不可逆动作的唯一证据（ADR-0003 因此把 `data/snapshots/` 列为受保护路径），而 `reconcile` 本身就是为「中途死亡」设计的恢复路径；恢复路径跑完之后证据仍停在意向态（`result: null`），等于在最需要证据的场景里，系统"恢复"了状态却没有恢复证据。库中状态显示已提交、快照显示未完成，两个载体从此长期不一致——而设计选择两个载体正是为了互为印证。<br>**关于我的漏检**：我通读了 `submit.py` 全文并逐条核对了 N8/N11/N14/N15，看到了 `snapshot_dir` 参数却没验证它是否被使用——只核「代码在不在」、没核「参数用没用」。这恰好是我在设计 R3 提醒过的同类错误（「替身若按理想情况返回数据，就会把取数路径的错误一并掩盖」），换成人工阅读同样成立：**看到参数就默认它被用了**。 | 高（同 A14） | 处置以 Architect A14 为准（`reconcile` 收敛后调用 `_patch_snapshot` 补齐对应快照）。DevOps 补两点验收要求：① 补齐时机应在**每条收敛后**而非全部结束后——reconcile 自身也可能中途死亡；② 断言用例除「快照被补齐」外，建议加一条「reconcile 后库中状态与快照结果一致」的交叉断言，直接测两个载体的互印性，而不只测其中一个被写过。 |
| O2 | **一键验证入口与 CI 已实质分叉成两套口径**。① `Makefile` 的 `PY := .venv/bin/python` 是硬编码——CI 里没有 `.venv`，`make check` 在 CI 中**跑不起来**，我此前建议的「CI 与一键入口共用同一入口」在当前实现下无法做到；任何未建 venv 的环境（换机、协作者）直接 `make test` 也会失败。② lint 排除规则不同：`Makefile` 排除 `docs/research|\.venv`，而我的 CI 只排除 `docs/research`。**已实证**：在含 `.venv` 的环境下跑 CI 那条命令，退出码 **1**（扫进 `.venv/.../_pytest/` 的 `match` 语法）。CI 当前无 `.venv` 故不受影响，但一旦给 CI 加 venv 缓存（很常见的提速手段）就会毫无征兆地红灯。③ 附带：自测报告「回归结论」称 `compileall` 检查「与 `l1-gates` 的 lint 步骤同口径」——**该表述不准确**，两者排除规则并不相同。 | 中 | 分两半处置：**Developer 侧**——`PY ?= $(shell [ -x .venv/bin/python ] && echo .venv/bin/python || echo python3)`，有 venv 用 venv、没有则回退，两种环境都能跑（`?=` 同时允许 `make PY=... test` 覆盖）；**DevOps 侧（我负责）**——把 CI 的 lint 排除规则对齐为 `docs/research\|\.venv`，并把两个步骤改为直接调用 `make lint` / `make test`，从此单一口径、不会再漂移。建议在实现阶段收尾时一并做掉，我可承接 CI 那半边。 |

**确认到位、无需修改**（DevOps 视角）

- **`confirm` 侧的不可逆动作实现忠实于三轮 Review 的结论**：⓪①②③ 四步顺序与设计逐字对应；`reconcile` 的判据是 `client.get_alpha` 直查、明确注释「不经本地库、不经水位」；查询失败 `continue` 保持 `in_flight` 并计入 `unresolved`——**不猜测、不收敛**落到了代码里，不是文档里的口号。（缺的那一步见 O3。）
- **`_refresh_alpha` 顺带刷新 `alpha` 表**（D24）用的是已在手的 `detail`，零额外请求——修一个低级意见没有引入新的平台调用。
- **`submit_alpha` 遇 401 只重试一次**（不进重试循环）：401 表示平台未受理请求、无副作用，重试安全；而对唯一的不可逆动作不做多次重试，这个保守选择是对的。
- **六条「亮绿灯但数据错」路径全部有针对性用例**，第 6 条（D21/N14）的替身构造正是设计 §8 要求的形态——`list_alphas` 不返回该 alpha、`get_alpha` 返回 `stage='OS'`，精确复现「常规链路拿不到、直查能拿到」的差异。这条用例本身就是那次 Review 的价值兑现。
- **测试零真实平台请求、零不可逆动作**，CI 可安全反复跑。

**关于那处设计偏离（认证探活 → 401 自愈）——DevOps 表态支持，裁定权仍在 Architect**

从运维角度：按字面每次探活会让全量同步的请求数翻倍（约 100+ 次业务请求配 100+ 次探活），在一个已知有限流风险、且最坏耗时以小时计的长任务上，这是实打实的成本与限流暴露面翻倍。而 §3.1.1 自陈的目标是「无需实测 session 生命周期，**按需**重认证」——401 触发正是「按需」的精确形态，目标达成且更省。异常闭合也已核过：重认证后仍失败会抛 `AuthError` → 退出码 2，不会静默死循环。

**放行条件**

- **O3 必修**（与 Architect A14 同源，处置以 A14 为准，我补两点验收要求）。
- **O1 是 [P0] 规定的推进前置**：push 并确认 `l1-gates` 绿灯之前，实现阶段不得推进到部署就绪检查/迭代关闭。修法只是 push，且已预演绿灯。注意 R2 修订后需**重新**取得绿灯——门禁结论绑定 commit，不继承。
- **O2 建议随 R2 一并处置**，CI 那半边由我承接。
- 除 O3 外，DevOps 侧对代码无其他必修项：产物落位、凭据纪律、`confirm` 侧证据链、六条路径覆盖均已到位。

> 备注：本报告「Review 状态」表与 `v0.1.md` 实现阶段门禁行的 DevOps 结果归 Developer 维护，Review 方不改正文，请 Developer 在处置时更新。

### R1 · Architect（架构师）· 2026-08-16 · 结论：需修改（A14/A15 高）

**独立性**：本会话是设计文档的产出方，但本轮被 Review 的对象是**实现代码**（Developer 产出），非本会话产出物，不构成自审。角色手册「审实现：是否符合设计、是否破坏边界、是否引入不可接受的技术债」正是本轮职责。

**审查方式**：逐文件通读 `alpha_platform/` 全部 10 个模块（约 1560 行），对照设计 §2 数据模型 / §3 接口契约 / §4 核心流程逐项核；独立复跑 `make test`（93 passed）；对三处疑点做了实机验证而非推断。

---

#### 一、偏离裁定（Developer 提请的 1 处）

**裁定：接受**，不要求改回字面实现。

设计 §3.1.1 写「每个公开方法进入时先探活」，实现改为「首次认证 + 401 自愈」。理由成立且比字面实现更好：

- §3.1.1 自陈的目标是「无需实测 session 生命周期，**按需**重认证即可覆盖长任务中途失效」——401 触发正是「按需」，目标完全达成。
- 字面实现会让全量同步的请求数翻倍（1 万条约 100+ 次业务请求 → 再加 100+ 次探活），而探活本身也会失败、也会被限流，等于把故障面扩大一倍去换一个 401 就能拿到的信息。
- Demo 每次探活是因为它是 MCP 工具、两次调用间可能隔很久；我们是连续批量请求，401 触发更贴合。
- 关键前提已满足：401 后**重试原请求**（`_request_json` 的 `continue` 走下一轮 `_send`；`submit_alpha` 单独写了一次重试），不会丢请求。

**流程处置**：按 `standard-iteration-quick.md` §11，这属于「实现取舍（不改契约）→ 不改定稿，写实现记录」的最轻一档。**设计文档不改**，本裁定即为留痕，请在迭代记录实现阶段行一并注明，避免后来者按设计字面改回去。

#### 二、缺陷

| # | 严重度 | 位置 | 问题 | 建议 |
|---|--------|------|------|------|
| A14 | 高 | `submit.py::reconcile` | **`--reconcile` 不补齐快照，`snapshot_dir` 参数形同虚设**。函数签名收了 `snapshot_dir`，函数体内**一次都没用到**（已 grep 确认：仅签名出现 1 次）。设计 §4.6 明确要求「原地补齐对应快照的逐条结果（**补上中途死亡时缺失的步骤 ③**）」，DevOps 在设计 R4 复核时也把「快照会永久停在意向态」列为 N11 要解决的一半。当前行为：提交中途进程死亡 → 快照的 `result`/`error` 字段永远是 `null` → 而快照是这个不可逆动作的**唯一证据**（ADR-0003 正是因此把 `data/snapshots/` 列为受保护路径、判据是「不可由库文件重算」）。自测 `test_reconcile_按平台真实状态收敛为已提交` 只断言了库中状态收敛，没断言快照补齐——**又一条「亮绿灯但功能缺失」**。 | `reconcile` 收敛每条后，按 `confirm_fingerprint` 定位对应快照文件并 `_patch_snapshot` 补齐该条结果；补一条断言「中途死亡 → reconcile 后快照结果字段非空」的用例。 |
| A15 | 高 | `client.py:118` + `sync.py:110/123/133` | **`count` 缺失被默认成 0，导致整窗数据静默丢失且窗口级对账仍判绿**。`AlphaListPage(count=body.get("count", 0))` 把「字段缺失」这一未知状态默认成「空」——危险方向的默认。下游 `while offset < reported` 不进循环 → `len(seen) == reported == 0` → 窗口标 `done`、`mismatched_slices` 为空 → 对账「通过」。**已实机验证**：构造一个 `count` 恒为 0 但 `items` 有数据的替身，`full_sync` 后 `alpha` 表 0 行、窗口 `status=done`、对账判定「通过」、不一致窗口数 0。当前不触发（平台实测返回 `count`），但失效模式与 D9/D16/D21 完全同型：静默、影响全量地基、以绿灯形态通过验收。 | `count` 字段缺失时抛 `PlatformError`，不默认成 0——「取不到总数」和「总数为 0」必须区分。这是一行的事，但方向对了：**不确定就报错，不要猜一个安全值继续**。建议一并列入 §8 第 6 条清单（成为第 7 条）。 |
| A16 | 中 | `precheck.py::run`、`cli.py:120` | **分批未实现**。`batch_size` 从 CLI 一路传到 `run()`，但函数体**未使用**（已 grep 确认：仅签名出现）。PRD #6 明确要求「执行须**分批**、单批规模可配」，当前是纯串行逐条。功能上因 autocommit 逐条落库 + 断点靠 `prediction_result` 而等价（中断可续、不重复请求），但验收 #6 的字面要求未满足，而自测报告标绿——这是我作为「验收标准独立复核」方必须指出的。 | 二选一：① 实现分批（批间可留节流点与进度输出，也是 R-1 长任务下 Owner 观察进度的抓手）；② 若判定串行已充分满足 PRD 意图，走 Change Note 把 #6 的「分批」措辞调整为实际形态——但不能让参数存在而行为缺失。 |
| A17 | 中 | `sync.py:44/53-66` | **设计 §4.1 步骤 5「全局对账」缺失**。步骤 0 取回了 `total_reported`（D9 的第三方左值，其全部价值在于**独立于切片路径**），但它只被赋值和在 CLI 打印，**从未与 `local_count` 比对、从未留痕**（已 grep 确认三处使用点）。设计写明「步骤 5：本地行数 == 步骤 0 的 total（不等则留痕）」，且明确「不判红、只留痕」。D9 修法的一半价值就落在这条上——外扩 1 秒修的是那一个 bug，第三方左值修的是「这类 bug 以后还能被发现」。 | 比对 `local_count` 与 `total_reported`，不等时写 `meta` 留痕（键名可仿 `offset_ceiling_detected`），并在 CLI 输出提示。仍不作为通过条件。 |
| A18 | 中 | `client.py::_extract_max_correlation` | **相关性取值只认 `schema.max` 一层**。Demo 的 `check_correlation` 写了三层兜底：`schema.max` → 顶层 `max` → 从 `records` 扫描（`platform_functions.py:1136` 起，我在 spike 中一手读过）——它专门写三层，说明平台响应格式存在变体。只取一层的后果不是报错，而是把「格式不同」误判为「平台还没算完」→ 重试耗尽 → **假待定**：白等 80 秒、占一个 `--retry-pending` 名额、Owner 以为平台慢。 | 至少补顶层 `max` 兜底；`records` 扫描可留待遇到再说。 |
| A19 | 中 | `precheck.py::_candidates` | **`--reset` 与常规候选集是互斥，而设计 §4.4 写的是 `OR`**——未申报的第二处偏离。**但实现比设计更安全**：设计的 `OR` 会让 `--reset A B` 在处理 A、B 的同时**触发整个常规候选集的预判**，那是几百条 × 80 秒的配额与时间。实现只处理点名的 ID，符合「点名重置」的用户预期。<br>**裁定：接受实现**，按 §11 属「实现取舍」，设计不改，本裁定即留痕。 | 无需改动。请在迭代记录实现阶段行与本报告一并注明，防止后来者按设计字面改回 `OR`。 |
| A20 | 中 | `classify.py::run` | **无事务包裹**。`db.connect` 用 `isolation_level=None`（autocommit），`run()` 逐条 `UPDATE` 各自提交——1 万条即 1 万次提交/fsync。对比 `sync.py::_upsert` 有 `_transaction` 批量包裹，两处对同量级写入的处理不一致。除性能外还有原子性问题：中途失败会留下「部分记录用新阈值、部分用旧阈值」的混合态，此时 `report` 给出的是混合结果（可重跑修复，但期间的数字会误导）。 | 整体或按批包一个事务。`precheck` 的逐条 autocommit 是**有意为之**（断点续跑），不要一并改。 |
| A21 | 低 | `report.py::_status_counts` | `if status in counts` 把计数为 0 的状态整个过滤掉，漏斗视图会缺级——Owner 分不清「提交失败 0 条」和「这个状态压根没出现过」。漏斗报告的价值恰在于完整的分级视图。 | 去掉该过滤，七态全列（「未回测」本就不在 `FUNNEL_STATUSES` 内，无需靠这个过滤排除）。 |
| A22 | 低 | `client.py::submit_alpha` | `submit_alpha` 不走 `_request_json`，因此**不享受 429 退避**：遇限流直接返回 `ok=False`，上层据此写 `funnel_status='提交失败'`——把一个**可恢复的暂时状态固化成终态**。该记录从此不在提交清单（清单取「待确认」），需人工处置。原因原文里有 `HTTP 429` 可查，故不算数据错误，但方向不对。 | 429 时退避重试；或至少不写「提交失败」终态，保留可重试状态。 |
| A23 | 低 | `submit.py::reconcile` IS 分支 | 退回「待确认」时写 `status_source='prediction'`，但该状态是 reconcile 造成的，不是预判。当前 `status_source` 只在「改造候选」拆行时被消费，故无功能影响。 | 改为 `'submit'`（或新增 `'reconcile'`）。 |
| A24 | 低 | `cli.py::main` | 无兜底 `except Exception → 1`。`PlatformError`、快照写盘 `OSError` 等靠**未捕获异常**退出，Python 默认退出码恰好是 1，行为满足 N12，但会向 stderr 打完整 traceback。（已核：traceback 不打印局部变量值，`Credentials` 也有 `repr=False`，**无凭据泄露风险**。） | 加兜底 `except Exception` → 打印简明错误 + 返回 1。 |

#### 三、验收标准 / 边界 / 回归的独立复核（我的指定职责）

- **#1**：窗口级对账落地正确（`fetched == reported` 逐窗口比对、不一致进 `mismatched_slices`）；根窗口外扩 1 秒、窗口重叠 1 秒、递归三约束（`mid` 向下取整 / ≤2s 不细分 / 深度 64）全部忠实实现。**但步骤 5 缺失（A17），且存在 A15 的静默路径。**
- **#6**：三态出口、短路、阈值带来源、`--retry-pending`、`--limit` 均正确。**分批未实现（A16）——本条标绿有水分。**
- **#8**：dry-run 零调用、指纹覆盖 `alpha_id:prediction_result`、`in_flight` 先落库再 POST、意向快照先落、前置检查退出码 6 —— 全部正确。**但 reconcile 不补齐快照（A14），证据链在最需要它的场景下是断的。**
- **#4/#5**：D16 修法落地准确——适用域只排保护态、`fail` 判断在 Python 侧（`row["prediction_result"] == "fail"`，`None` 比较为 False，无三值陷阱）；`classify_checks` 确为纯函数，#5 因此是纯单测。守恒断言恒成立（含「已回测」）。
- **#9**：`repr=False` 在契约层、白名单式日志、`credentials.py` 报错不回显文件内容、`_setup_logging` 只配包 logger 不碰 root（这处自测中发现并修正的问题抓得好）。
- **#2/#3/#7/#10**：正确。
- **回归**：独立复跑 `make test` = 93 passed，与报告一致；`compileall` 与 CI lint 同口径。**用例全部在替身层，零真实平台请求**——这一点对「唯一不可逆动作」的项目尤其重要，做到了。
- **边界**：四维自测覆盖扎实，D9/D11/D16/D21 四条针对性用例都真的对着缺陷的暴露条件写（尤其第 6 条：替身在 `list_alphas` 中不返回、`get_alpha` 返回 `OS`，这正是 N14 指定的构造）。**未覆盖的是 A14 的快照补齐与 A15 的 `count` 缺失。**

#### 四、确认到位、无需修改

- **`§3.1.0` 依赖注入契约被完整执行**：`cli.py` 是唯一组装点，`client_factory` 注入，全项目无模块级单例、无 monkey-patch。这是设计里我最担心会被打折扣的一条，落实得干净。
- **`raw_json` 单一真源 + 抽索引列**、双表分层、`ensure_state` 的 `ON CONFLICT DO NOTHING`——§2 与 §4.2 的字段分层由 schema 强制，实现没有绕过。
- **`config.py` 的阈值来源自动判定**：显式指定过 `correlation_threshold` 即自动标 `manual`，比设计要求更进一步——设计只要求「必带来源」，实现让来源**无法被忘记填**。这是超出契约的正向发挥。
- **`Makefile` 的口径提示**：`make test` 第一行就打印「验收 #1 按窗口级对账判定（CN-001 / 设计 §4.1 步骤 4）」，N13 的意图（把口径来源放进 Owner 实际会看的那份输出里）落到了实处。
- **CI workflow 的注释**：显式论证了「不设 paths-ignore」的理由（文档 commit 若拿不到绿灯，按 [P0] 会把流程卡死）——这个推理是对的，且是实现方主动补的。
- **自测报告的诚实度**：主动提请裁定偏离、写明「本机 Python 3.9.6 不满足设计要求故用 uv 建 3.11 环境」、记录自测中修正的 `basicConfig` 问题。报告没有粉饰。

#### 五、放行条件

A14、A15 为高，均需在定稿前修入实现：A14 是**设计明确要求的功能缺失**且断了不可逆动作的证据链；A15 是**已实机验证的静默丢数路径**，虽当前不触发但与前四轮反复修掉的同型问题一脉相承。A16–A20 建议一并处理（A16 涉及 PRD 验收字面，若选择不实现须走 Change Note）。A21–A24 可留作实现阶段收尾时清理。

两处偏离（认证探活、`--reset` 语义）**均已裁定接受**，设计不改，本记录即为留痕。

修订后 Architect 可在 R2 快速复核——A14/A15 的修法明确、无设计决策空间，不需重审全文。
