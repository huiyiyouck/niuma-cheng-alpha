# v0.1 PRD

## 文档状态
- 整体状态：待Review
- Review 轮次：R2
- 关联迭代：v0.1

## Review 计划
- Review 模式：动态 Review
- 影响领域：产品范围与验收（PM 产出）、数据模型与平台 API 边界（架构）、工程成本与可行性（开发）、凭据/不可逆动作/运行韧性（运维）
- 本轮指定 Review 方：
  - Architect（架构师）——库存库数据模型、与 BRAIN API 的同步/检查触发边界
  - Developer（开发工程师）——工程成本、分流规则实现可行性
  - DevOps（运维/部署工程师）——Owner 于 2026-08-15 追加（R1 起生效）
- 未指定角色及原因：无（三方已覆盖）
- 少于 2 个 Review 方时的用户确认：无（已指定 3 方）
- 定稿条件：本轮指定 Review 方全部通过，且无未处理高严重度阻塞问题

## Review 状态
| Review 方 | 指定原因 | 状态 | 轮次 |
|-----------|----------|------|------|
| Architect | 数据模型与 API 边界 | R1 不通过 → R2 待反馈 | R2 |
| Developer | 工程成本与可行性 | R1 需修改 → R2 待反馈 | R2 |
| DevOps | 凭据/不可逆动作/运行韧性 | R1 通过（附条件 H1/M2，已在 R2 处置；复核可选） | R1 |

## 1. 迭代目标

把 Owner 在 BRAIN 平台的全部 Alpha 资产（实测未提交 1 万+、已提交 88）同步为**本地漏斗库存库**，用平台 checks 做**规则化分流**，产出「可提交候选清单」，并支持人工确认后提交——打通漏斗右半段（已回测 → 分流 → 提交）的最短闭环。

本迭代不做组装、不做改造 AI、不做 Web 界面、不做服务器常驻（见范围边界与版本路线）。选择先做右半段的原因：库存里已有 1 万+ 已回测 Alpha 待处理，右半段闭环是当前对「提交数」这一成功指标见效最快的一段，也为后续改造回路（v0.2）与组装核心（v0.3）提供地基（库存库 + 分流引擎是两者的共同依赖）。

## 2. 用户故事

1. 作为 Alpha 研究员，我想把平台上全部 Alpha（未提交 + 已提交）同步到本地库存库，以便完整掌握自己的资产全貌而不受平台页面筛选能力限制。
2. 作为 Alpha 研究员，我想让系统按平台 checks 结果把库存自动分流为「可提交候选 / 改造候选 / 淘汰候选」，以便不用逐个翻看 1 万+ Alpha 也知道下一步推谁。
3. 作为 Alpha 研究员，我想对「可提交候选」批量获取相关性数据并得到**提交前预判**（通过 / 不通过 / 待定），以便在消耗提交机会之前过滤掉大概率被拒的候选（最终裁决以平台提交结果为准）。
4. 作为 Alpha 研究员，我想看到漏斗各级数量与卡点分布（哪条 check 挂得最多、差距多少），以便判断库存整体质量与后续改造方向。
5. 作为 Alpha 研究员，我想在人工确认最终清单后由系统执行提交并回写结果，以便安全地完成「筛选 → 提交」收尾（提交是对外不可逆动作，必须人工确认，不自动提交）。

## 3. 漏斗状态机与分流规则（R2 新增，A2/A3/D1/D3/Q2 处置）

### 3.1 漏斗状态枚举（库存记录唯一状态字段，#4/#6/#7 统一引用本表）

| 状态 | 进入条件（迁移触发） |
|------|---------------------|
| 已回测 | 同步入库的初始态（stage=IS 的平台 Alpha 均自带回测结果） |
| 可提交候选 | 分流：checks 无 FAIL |
| 改造候选 | 分流：有 FAIL 且全部可量化 FAIL 差距在改造阈值内；或提交前预判不通过（理由=相关性超阈） |
| 淘汰候选 | 分流：任一可量化 FAIL 差距超过改造阈值 |
| 待确认 | 可提交候选经提交前预判通过 |
| 已提交 | 人工确认后提交成功（stage=OS 的平台 Alpha 同步入库时直接置此态） |
| 提交失败 | 提交被平台拒绝（失败原因回写，人工处置） |

- 「未回测」不设：v0.1 库存全部来自平台已回测产物，该级恒为 0；v0.3 组装端接入时再新增。
- 预判「待定」（详见验收 #6）不是独立状态：保持「可提交候选」并加待定标注。
- **字段分层（Q2/M4）**：平台原始字段（表达式 / 设置 / 指标 / checks）不可变，同步时可覆盖刷新；本地派生字段（漏斗状态 / 分流理由 / 预判结果 / 确认留痕 / 提交结果）只由本地流程写入，同步不得触碰——分流结果永远可由原始数据重算。

### 3.2 分流规则（v0.1 初版）

- **门槛项**：checks 中 `result ∈ {PASS, FAIL}` 的检查（平台已下判定的硬检查）。`WARNING / ERROR / PENDING` 一律**非门槛**：不参与分流判定，但必须在分流理由中单独留痕（CLUSTER_TEST 全 ERROR、UNITS 常 WARNING 由此规则覆盖，不再单点处理）。
- **差距定义**：`gap = |value − limit| / |limit|`（FAIL 项的相对差距；LOW_* 类 value < limit，HIGH_* 类 value > limit，取绝对值统一）。无 value/limit 的不可量化 FAIL 视为差距在阈值内（保守归改造候选，理由中标注「不可量化」）。
- **改造阈值**：默认 0.3（可配置项，Owner 可调）。
- **分流判定**（对每条「已回测」执行，全覆盖无残留）：
  1. 无 FAIL → **可提交候选**
  2. 有 FAIL 且全部可量化 FAIL 的 gap ≤ 改造阈值 → **改造候选**
  3. 任一可量化 FAIL 的 gap > 改造阈值 → **淘汰候选**
- **分流理由**（每条必存）：触发的 check 名、result、value/limit 与 gap、非门槛项留痕清单。
- 达标线（limit）一律取平台 checks 返回值，不硬编码（与 project-context 硬约束一致）。

## 4. 验收标准

> **验收可执行化（BCR-015 L1）**：每条验收标准必须填「验证方式」——能自动验证的填可执行命令 / 测试入口；确实无法自动验证的显式标 `人工抽检` 并写原因。**不允许留空**：标不出验证方式的验收标准本身就是 PRD 缺陷，Review 时按缺陷处理。界面类验收允许「构建通过 + 截图留档 + 人工抽检」组合，不强求端到端自动化。

| # | 验收标准（当…时，应该…） | 验证方式 | 验证入口（命令 / 测试文件路径；`人工抽检` 须附原因） |
|---|--------------------------|----------|--------------------------------------------------|
| 1 | 当执行全量同步时，应该把可获取的全部未提交与已提交 Alpha 拉入本地库存库；对账采用**双路可证伪口径**：按 offset 分页穷尽的去重条数 == 按 dateCreated 时间窗切片求和的去重条数，且连续两次穷尽结果一致。两路不一致时应判定「offset 存在封顶」并留痕，同步以时间窗切片口径为准 | 自动 | 同步命令 + 双路对账测试（offset 穷尽 vs 时间窗切片求和） |
| 2 | 当执行增量同步时，应该按 dateCreated 只拉取上次同步后**新增**的 Alpha（增量 = 新增；已入库 Alpha 的平台字段刷新由全量重拉或 #6 预判回写承担，不属增量职责），且重复执行幂等（条数与内容不变） | 自动 | 幂等性测试（连续两次增量同步，条数与内容不变） |
| 3 | 当同步完成后，应该每条 Alpha 都带有表达式、仿真设置、指标与 checks 结果，可按漏斗状态查询；且平台原始字段与本地派生字段分层存储——再次同步只刷新平台字段，派生字段（状态/理由/预判/确认/提交结果）保持不变 | 自动 | 抽样字段完整性测试 + 「同步后派生字段保留」断言 |
| 4 | 当对库存执行分流时，应该每条「已回测」Alpha 按 §3.2 规则被标记为「可提交候选 / 改造候选 / 淘汰候选」三者之一，分流理由（触发 check、result、value/limit 与 gap、非门槛项留痕）可查询 | 自动 | 分流命令 + 全量覆盖测试（无未分类残留）+ 理由字段非空校验 |
| 5 | 当分流规则的达标线来源于平台时，应该随 region/universe 动态读取而非硬编码，平台调整门槛后重新分流结果随之变化 | 自动 | 规则单测（注入不同 limit 的 checks 数据，断言分流结果变化） |
| 6 | 当对可提交候选执行**提交前预判**时，应该分批获取相关性数据（self/prod 相关性接口），按阈值给出预判结论（通过 → 待确认 / 不通过 → 改造候选 / 达重试上限仍无结果 → 标待定、不阻塞其余候选）并回写；阈值必须带**来源字段**（平台值 / 惯例值 0.7 / 手工配置，不允许无来源硬编码）；执行须分批、单批规模可配、断点续跑、已获取结果不重复请求 | 自动（mock/录制回放注入相关性样例，断言预判与回写逻辑）+ 人工抽检（真实小批量执行；原因：消耗平台配额且结果不可重复） | 预判命令 + 回放测试 + 断点续跑测试 |
| 7 | 当查看漏斗报告时，应该能看到 §3.1 各状态数量与 FAIL 卡点分布 TOP 榜（哪条 check 挂得最多、平均 gap）；不含「未回测」级（v0.1 无数据源，v0.3 占位） | 自动 + 人工抽检 | 报告生成命令（自动断言各级数字与库存一致）；报告可读性人工抽检（原因：可读性无法自动断言） |
| 8 | 当未经人工确认时，系统不应该向平台提交任何 Alpha；应该提供 **dry-run 模式**（打印将提交清单、提交接口零调用）；人工确认针对**清单内容指纹**（清单生成时计算指纹，Owner 以显式命令对指纹确认；清单内容变化则确认失效）；确认后逐个提交、回写结果、失败项留明确原因；每次提交生成**清单快照落盘**（时间、Alpha ID、指纹、确认留痕、逐条结果） | 自动 | 提交流程测试（无确认标记零调用 + dry-run 零调用 + 指纹失效断言 + 模拟提交回写断言 + 快照存在性断言）；真实提交由 Owner 在验收时抽验 |
| 9 | 当凭据文件缺失或认证失败时，应该给出明确报错并终止；认证材料（请求体、Authorization 头、session cookie）一律不落盘；日志与库存文件中不出现 `password` / `credentials` 字段值与 Basic 认证串特征；测试自身不读取凭据文件内容 | 自动 | 异常路径测试 + 结构化扫描断言（按字段名与认证串特征扫描，不接触凭据明文） |
| 10 | 当全量同步中断（网络失败 / 认证失效 / 手动中止）后重跑时，应该从断点继续，已拉取部分不重复请求、不从头重来 | 自动 | 模拟中途异常后重跑，断言已拉取分片不再请求 |

## 5. 范围边界

**本迭代做：**
- 本地库存库：全量 + 增量同步（未提交 + 已提交），含表达式 / 设置 / 指标 / checks
- 规则分流引擎：按 §3.2 规则三分类 + 分流理由留痕；达标线动态读取
- 提交前预判：对可提交候选分批获取相关性数据并按有来源阈值预判（见验收 #6）
- 漏斗报告：§3.1 各状态数量 + 卡点分布（命令行 / 静态报告形态即可）
- 人工确认（指纹绑定）后的批量提交与结果回写，含 dry-run 与快照落盘

**设计约束（实现须遵守，Architect 设计阶段落实）：**
- 平台原始数据不可变、本地派生数据可重算（备份 = 复制库文件，误操作恢复 = 重算）（M4）
- 平台 API 访问收敛到可注入替身的单一客户端层（支撑 #5/#6/#8/#9 的自动化验证）（Developer 实现前提）
- 凭据文件路径允许环境变量覆盖（如 `BRAIN_CREDENTIALS_PATH`），默认项目根（L1，为 v0.2 常驻预留）

**本迭代不做：**
- 组装核心（生成新 Alpha）——v0.3 方向
- 改造回路（病历 → 药方，含 LLM）——v0.2 方向；v0.1 的「改造候选」只分类不改造
- Web 管理界面——漏斗报告先以命令行/静态产物承载，Web 面等库存与分流数据稳定后再建
- 服务器常驻 / 定时调度——v0.1 手动触发命令即可
- 收益追踪（Brief 已定：范围外）
- Agent Hub 接入（预留，不实现）

**版本路线（规划参考，非承诺）**：v0.1 右半段闭环（本迭代）→ v0.2 改造回路 + 常驻调度 → v0.3 组装核心 + Web 管理面。

## 6. 前置依赖

- BRAIN 平台账号与项目内凭据文件 `.brain_credentials`（已就位，已入 .gitignore）
- 平台 API 事实调研（已完成：`docs/research/brain-platform-facts.md`）
- 技术栈选型（Architect 在设计阶段决定；PRD 不指定实现）

## 7. 风险和开放问题

1. **offset 封顶疑问已转为可证伪验收**（A4/D2 处置）：#1 双路对账两路不一致即证实封顶并留痕，同步策略以时间窗切片为准（设计阶段定具体切片方案）；「平台实际总数」口径已删除。
2. **预判的责任边界**（A1 方案 B 处置）：`DATA_DIVERSITY` / `REGULAR_SUBMISSION` / `POWER_POOL_CORRELATION` 无已验证的获取路径，v0.1 不预判这三项——**最终裁决 = 平台提交结果**，提交失败原因回写即闭环。设计阶段 Architect 仍应查证「提交前检查触发端点」是否存在；探明可得后经 Change Note 将 #6 升级为真判定。
3. **提交动作默认按不可回滚对待**（H1 处置）：平台是否支持撤回已提交 Alpha 由设计阶段查证回填；查证前一律按不可回滚执行——dry-run + 指纹确认 + 快照落盘三重兜底，Owner 验收时知情。
4. **非 PASS/FAIL 检查项**由 §3.2 普遍规则覆盖（非门槛、留痕），CLUSTER_TEST 含义查证降为观察项，不阻塞。
5. **认证 session 生命周期与限流阈值未实测**（M3 处置）：设计阶段必答项——影响 #6 分批节流与 #10 断点续跑的参数设计。
6. **CI 门禁待 Owner 拍板**（M1，已登记跨任务待办）：(a) 本迭代建最小 `l1-gates`（单测 + lint，不触真实平台）或 (b) 明示接受 v0.1 全程降级模式（逐阶段 Owner 确认 + 降级留痕）。

## R1 → R2 修订对照（PM，2026-08-15）

| 意见 | 处置 | 落点 |
|------|------|------|
| A1（阻塞）PENDING 检查缺 API 路径 | 选**方案 B**：#6 改「提交前预判」口径，阈值带来源字段；无路径的三项检查交平台提交裁决；触发端点查证列设计阶段任务，探明后 Change Note 升级 | 验收 #6、风险 2 |
| A2 / D3 状态枚举不自洽、「未回测」无来源 | 新增 §3.1 唯一状态机（7 态 + 迁移条件），#4/#6/#7 统一引用；「未回测」删除，v0.3 占位 | §3.1、验收 #7 |
| A3 / D1 分流规则未定义 | 新增 §3.2 规则表：PASS/FAIL 为门槛项，WARNING/ERROR/PENDING 非门槛留痕；gap 定义 + 改造阈值默认 0.3 可配；不可量化 FAIL 保守归改造候选 | §3.2、验收 #4 |
| A4 / D2 #1 自证循环、不可判定 | 改双路对账可证伪口径（offset 穷尽 vs 时间窗切片），「平台实际总数」删除 | 验收 #1、风险 1 |
| A5 / Q1 增量「变更」无依据 | 增量语义定为「增量 = 新增（dateCreated）」；平台字段刷新由全量重拉 / #6 回写承担 | 验收 #2 |
| A6 / Q3 确认载体未定义 | 确认 = 对清单内容指纹的显式命令确认，清单变化确认失效；粒度为整批清单 | 验收 #8 |
| A7 / D4 / M3 批量成本与韧性 | #6 补分批/可配/断点续跑/不重复请求；新增 #10 同步断点续跑；session/限流列设计必答 | 验收 #6/#10、风险 5 |
| A8 凭据断言口径 | 改结构化断言：认证材料不落盘 + 按字段名/认证串特征扫描，测试不读凭据明文 | 验收 #9 |
| Q2 / M4 同步与派生字段覆盖冲突 | 字段分层：平台原始字段可刷新、派生字段同步不触碰、派生可重算 | §3.1、验收 #3、设计约束 |
| Q4 PENDING 无终止条件 | 预判三态出口：通过 / 不通过 / 达重试上限标待定（不阻塞其余） | 验收 #6 |
| H1 不可逆缺回滚定义 | 默认按不可回滚对待 + dry-run + 指纹确认 + 快照落盘；平台撤回能力设计阶段查证 | 验收 #8、风险 3 |
| M1 CI 门禁不存在 | 交 Owner 拍板（二选一），已登记 INDEX 跨任务待办 | 风险 6 |
| M2 #6 自动验证触真实平台 | 自动部分改 mock/录制回放，真实小批量标人工抽检（附原因） | 验收 #6 |
| L1 凭据路径硬编码 | 列设计约束：环境变量可覆盖，默认项目根 | §5 设计约束 |

## Review 记录

### R1 · Architect（架构师）— 2026-08-15

**结论：不通过（需进入 R2 修改）**

**Review 范围**：技术可行性、依赖风险、架构冲突；验收/边界/回归复核（PM 产出验收标准，由 Architect 复核防自审）。

**依据材料**：`docs/research/brain-platform-facts.md`（平台 API 实测事实）、`docs/research/AI打工人_demo/platform_functions.py`（已验证的 API 调用面）、`docs/baseline/project-context.md`（项目硬约束）。

**总体判断**：迭代目标（先做漏斗右半段）方向正确，库存库 + 分流引擎作为 v0.2/v0.3 共同地基的取舍成立，范围边界清晰。但验收标准中有 1 条建立在**未验证的平台能力**上，另有 4 条存在**口径不自洽或不可证伪**的问题，按当前文本无法进入设计阶段。

#### 问题清单

| # | 问题 | 严重度 | 涉及 |
|---|------|--------|------|
| A1 | 「获取 PENDING 类检查最终结果」缺已验证的 API 路径，且本地判定阈值与「达标线动态化」硬约束冲突 | 阻塞 | 验收 #6、风险 2 |
| A2 | 漏斗状态枚举三处不自洽，且「未回测」在 v0.1 无数据来源 | 高 | 验收 #4/#6/#7 |
| A3 | 分流规则对 WARNING / ERROR / PENDING 的处理未定义，与「无未分类残留」硬验收冲突 | 高 | 验收 #4、风险 4 |
| A4 | 验收 #1 的验证方式自证循环，无判别力 | 高 | 验收 #1、风险 1 |
| A5 | 增量同步的「变更」维度无实现依据（平台无 dateModified 过滤） | 高 | 验收 #2 |
| A6 | 「人工确认」的载体未定义，直接决定不可绕过性与可测性 | 中 | 验收 #8 |
| A7 | 提交前检查的批量规模 / 限流 / 中断恢复未落到验收或范围 | 中 | 验收 #6、风险 2 |
| A8 | 凭据扫描断言的判定口径未定义，按字面实现会与凭据纪律冲突 | 中 | 验收 #9 |

---

**A1（阻塞）· 验收 #6 缺可行 API 路径**

验收 #6 要求「逐个获取 PENDING 类检查（相关性/多样性等）的**最终结果**并回写库存」。但：

1. 已入库事实（`brain-platform-facts.md` §2 第二梯队）只记录了这些 check **初始为 PENDING**，未记录任何把它们推到最终结果的接口。
2. demo 源码中与之相关的只有 `GET /alphas/{id}/correlations/prod` 与 `/correlations/self` 两个接口，返回的是**相关性原始数据**（`schema.max`），不是平台的 check 判定；demo 的 `check_correlation()` 用**本地硬编码 0.7** 作阈值（`platform_functions.py:1136`）。
3. 该阈值来源与 project-context「达标线动态化（不得硬编码）」硬约束正面冲突——0.7 是 demo 惯例值，不是 API 返回的 `limit`。
4. PENDING 类中的 `DATA_DIVERSITY` / `REGULAR_SUBMISSION` / `POWER_POOL_CORRELATION` 三项，事实库与 demo 均无任何获取路径。

即：v0.1 核心闭环最后一环所依赖的平台能力**尚未被验证存在**。不排除平台有未探明的接口（如提交前检查触发端点），但在验证前 #6 不可承诺。

建议二选一（PM 决策，非 Architect 决策）：
- **方案 A（推荐）**：把「PENDING 检查探明」作为设计阶段的**前置 spike**（Architect 执行，非迭代预研，结论回填 PRD）。探明后按实际能力重写 #6；探明为不可得时改口径。
- **方案 B**：现在就把 #6 降级为「**预判**」口径——「对可提交候选获取相关性原始数据并回写，按记录了来源的阈值给出提交前预判；最终裁决以提交结果为准」，并把阈值来源（平台值 / 惯例值 / 手工配置）作为必填字段写进验收，以满足「不硬编码」的可审计要求。

---

**A2（高）· 漏斗状态枚举不自洽**

同一份 PRD 出现三套状态口径：#4 是三分类（可提交候选 / 改造候选 / 淘汰候选），#6 引入第四态「最终可提交清单」，#7 的报告要求六级（未回测 / 已回测 / 可提交候选 / 改造候选 / 淘汰候选 / 已提交）。数据模型只能有一套枚举，这直接决定库存库的状态字段设计。

另外「未回测」一级在 v0.1 **没有数据来源**：库存全部来自平台已有 Alpha 列表，而平台上的 Alpha 均为仿真产物、自带 `is` 指标与 checks；组装（产生未回测 Alpha 的唯一来源）明确排除在本迭代外。该级恒为 0。

建议：PM 给出**唯一的漏斗状态枚举 + 迁移条件**（谁→谁、由哪个动作触发），三条验收统一引用；「未回测」要么删除，要么注明「v0.1 恒为 0，为 v0.3 组装预留」。

---

**A3（高）· 分流规则对非 PASS/FAIL 结果未定义**

`result` 有五种取值（PASS / FAIL / WARNING / ERROR / PENDING），而 #4 是硬验收：「每条已回测 Alpha 被标记为三者之一，**无未分类残留**」。当前 PRD 只在风险 4 单点处理了 `CLUSTER_TEST`（计为非门槛项），没有给出普遍规则。实测中 `UNITS` 常为 WARNING、`CLUSTER_TEST` 样本全为 ERROR，这不是边缘情况。

规则不完备则「无未分类残留」不可达，或者实现方会自行编造默认值——这是最该由 PRD 而非 Developer 决定的口径。

建议：PM 明确「五种 result 各自如何计入分流」的普遍规则（例如：只有 FAIL 计门槛不达标，WARNING/ERROR/PENDING 一律计为非门槛项且在分流理由中单独留痕），CLUSTER_TEST 由普遍规则覆盖，风险 4 相应降为观察项。

---

**A4（高）· 验收 #1 的验证方式自证循环**

#1 要求「本地条数与**平台实际总数**一致」，验证方式是「本地计数 vs **平台分页穷尽计数**」。当同步实现本身就是分页穷尽时，两个数字必然相等——无论平台是否在 10000 处封顶，该验收恒为绿灯，**不可证伪**，无法回答风险 1 想回答的问题。

可用事实：列表 API 支持 `dateCreated>` / `dateCreated<` 时间窗过滤（`platform_functions.py:504-507`），这提供了一条**独立于 offset 分页的对账路径**，也是绕过 offset 上限的可行技术方案。

建议：
- 验收口径改为可证伪的表述：「按 offset 分页穷尽的条数 == 按 dateCreated 时间窗切片求和的条数」，两路不一致即判定 offset 存在封顶并留痕；
- 「平台实际总数」这一无法独立观测的口径删除，或降级为 Owner 人工抽检项；
- 若时间窗切片求和 > 10000，风险 1 即被证伪，同步策略以时间窗切片为准（这也是我在设计阶段的倾向方案）。

---

**A5（高）· 增量同步的「变更」维度无实现依据**

#2 要求「只拉取上次同步后**新增/变更**的 Alpha」。列表 API 已验证的过滤参数只有 `dateCreated>/<` 与 `dateSubmitted>/<`，**没有 dateModified 或等价的修改时间过滤**。因此「新增」可增量，「变更」（checks 重算、指标更新、属性修改）**无法增量识别**，只能靠周期性全量重拉比对。

#2 的验证方式（幂等性测试）只能覆盖「重复执行不产生重复记录」，覆盖不到「变更被正确识别」——验收与标准本身不匹配。

建议：PM 明确增量语义。倾向表述：「增量同步 = 按创建时间拉新增；变更通过全量重拉刷新，且重复执行幂等」。若确需变更增量，需先探明平台是否存在修改时间字段（可并入 A1 的 spike）。

---

**A6（中）· 「人工确认」的载体未定义**

#8 是本迭代唯一的不可逆动作闸门（对应 project-context 硬约束「提交不可逆」），但 PRD 未定义人工确认的**形态**：命令行交互输入、独立的确认清单文件、还是清单指纹签名。这不是实现细节——它决定「确认标记」能否被绕过、能否被误复用（例如确认了清单 A 却提交了变更后的清单 B），也决定 #8 前半句「无确认标记时提交接口零调用」怎么测。

建议：PM 给出确认载体与**确认对象绑定方式**（推荐：确认针对具体清单内容的指纹，清单变化则确认失效）。架构侧的可注入客户端替身由我在设计阶段承接，不需要 PRD 展开。

---

**A7（中）· 提交前检查的批量执行成本未落到验收或范围**

风险 2 已识别限流风险，但没有任何验收或范围条款承接它。已知事实：相关性接口是**逐 Alpha 调用**，且 demo 对其实现了「空响应重试 + 退避等待」（`platform_functions.py:1016-1135`），说明平台侧为异步计算、首次调用常返回空。若「可提交候选」有数百条，#6 会变成一个耗时不可控的长任务。

不写进 PRD 的直接后果是：实现阶段大概率产出一个跑几小时、中断即重来的脚本。

建议：#6 补充可控性口径——「分批执行、单批规模可配、支持中断后断点续跑、已获取结果不重复请求」，并作为验收的一部分。

---

**A8（中）· 凭据扫描断言的判定口径未定义**

#9 的验证入口写「日志/库文件内容扫描断言」，但未定义**扫描什么**。若按字面实现为「断言日志中不出现密码明文」，测试代码就必须读取 `.brain_credentials` 内容——与 project-context「不读取内容入日志/文档」的凭据纪律冲突，且明文可能进入测试快照。

建议：改为不接触凭据内容的结构化断言，例如「日志与库文件中不出现 `.brain_credentials` 的任何一行内容（由测试在内存中比对后立即丢弃）」或更安全的「认证请求体、`Authorization` 头、session cookie 一律不落盘；日志中不出现 `password` / `credentials` 字段名」。具体口径 PM 定，我在设计阶段落实现约束。

#### 未列为缺陷但已记录（Architect 在设计阶段承接，PRD 无需改）

- 提交结果回写口径：`POST /alphas/{id}/submit` 返回后平台侧仍可能异步产出最终结论，「提交成功」的判定标准需在设计文档定义。
- 库存库存储选型：1 万+条 × 完整 settings / code / checks / 多视图指标的数据量级会影响选型，属设计阶段决策。
- 可注入的平台客户端替身（支撑 #8「零调用」断言与 #5 的 limit 注入测试）作为设计阶段的接口契约要求。

#### 给 PM 的最小修改清单（R2）

1. A1：选定方案 A（前置 spike）或方案 B（改「预判」口径）——**这条不解决不能进设计阶段**。
2. A2：给出唯一漏斗状态枚举 + 迁移条件，统一 #4/#6/#7；处置「未回测」级。
3. A3：给出五种 `result` 的普遍分流规则。
4. A4：#1 改为可证伪口径（两路对账）。
5. A5：明确增量语义，#2 与验证方式对齐。
6. A6/A7/A8：补确认载体、批量可控性、凭据断言口径。

### R1 — DevOps（运维/部署工程师） · 2026-08-15

- **Review 方追加来源**：本 PRD「Review 计划」原以「v0.1 不含部署上线（本地运行）」为由未指定 DevOps；Owner 于 2026-08-15 口头追加 DevOps 为 R1 Review 方。本记录据此产生。正文「Review 计划 / Review 状态」两处归 PM 维护，Review 方不改正文，请 PM 在定稿时补入 DevOps 行。
- **独立性**：本会话为冷启动 DevOps 会话，未参与 PRD 任何产出，不构成自审。
- **审查范围**（按角色手册）：部署方式、环境变量与凭据、外部服务依赖、发布风险、回滚条件；不评产品范围与技术选型。
- **结论**：**通过（附条件）** — 无阻塞级缺陷。v0.1「本地运行、不部署」的定位成立，DevOps 不设部署阻塞。附条件：H1、M2 请 PM 在定稿前处理或明确延期记录。

| # | 问题 | 严重度 | 建议 |
|---|------|--------|------|
| H1 | **唯一不可逆动作缺回滚路径定义**。提交是 PRD 内唯一对外不可逆动作，验收 #8 建了「人工确认」前置闸，但全文未定义提交后的撤销/纠错路径，也未要求提交清单留档。DevOps 部署检查清单第 5 项「回滚准备」在此处无对应内容。 | 高（本迭代修） | ① 设计阶段须确认 BRAIN 平台是否支持撤回/删除已提交 Alpha；不支持则在 PRD 明示「提交不可回滚」，让 Owner 在验收时知情。② 验收 #8 补两项可自动验证的要求：**dry-run 模式**（打印将提交清单、零调用提交接口）与**提交清单快照落盘**（含时间、Alpha ID、确认动作留痕），使误提交至少可事后追责与人工善后。 |
| M1 | **项目无 CI，`l1-gates` 门禁不存在**。已核实仓库无 `.github/` 目录。按 `runtime.md` [P0]，阶段推进唯一依据是约定名门禁 `l1-gates` 的绿灯结论，「不存在」属非绿灯 → v0.1 将**全程运行于降级模式**，每阶段推进都需 Owner 确认并在迭代记录写降级留痕。同时 9 条验收标准中 8 条标「自动」，需要实际可执行的测试入口与运行环境。 | 中 | 请 Owner 拍板二选一：**(a)** 本迭代内建最小 `l1-gates`（建议实现阶段前由 DevOps 承接，只跑单测 + lint，不接触真实平台）；**(b)** 明示接受全程降级模式，由 PM/各阶段角色逐阶段登记降级留痕。两者都可接受，但需显式选择——默认漂移成 (b) 而无留痕才是流程缺陷。 |
| M2 | **验收 #6 标「自动」但会对真实外部系统产生副作用**。「对小批量真实候选执行」触发平台 PENDING 类检查属真实 API 调用，消耗账号配额、受限流影响、结果不可重复，不适合作为可反复执行的自动测试。对比验收 #8 已正确区分「模拟回写断言（自动）+ 真实提交由 Owner 抽验」，#6 写法与之不一致。 | 中 | 按 #8 的写法对齐拆分：自动部分改为 **mock / 录制回放**（注入 PENDING→最终结果的样例数据，断言回写逻辑）；真实小批量执行标为 **人工抽检**（原因：真实调用消耗平台配额且结果不可重复）。 |
| M3 | **长任务韧性未覆盖**。全量同步 1 万+ 条是长耗时任务，而 `docs/research/brain-platform-facts.md` 中**认证 session 生命周期、限流阈值均无实测记录**（该轮调研仅做只读拉取）。风险 2 只覆盖了「提交前检查」的配额，未覆盖同步本身。中途认证失效或被限流时的行为未定义。 | 中 | 验收 #2 的幂等性只保证「重复执行不产生重复记录」，不等于「中断可续」。建议补一条验收：**全量同步中断后重跑应从断点继续，不从头重来**（验证方式：模拟中途异常后重跑，断言已拉取部分不重复请求）；或在风险表把「认证有效期与限流阈值未实测」显式登记为设计阶段必答项。 |
| M4 | **唯一有状态资产无备份/恢复要求**。本地库存库是 v0.1 唯一持久化资产，同步与分流均会覆写状态。验收 #4 要求分流理由可查询，但无历史版本——规则调整后重跑，旧分流结果与理由被覆盖且不可追溯。 | 中 | 不必引入版本化存储（v0.1 过重）。最低要求写成设计约束即可：**平台原始数据（表达式/设置/指标/checks）不可变，分流结果为可重算的派生数据**。满足此约束时「备份」退化为复制单个库文件，误操作恢复 = 重算，成本极低。 |
| L1 | **凭据文件路径硬编码在项目根**。`.brain_credentials` 固定于项目根目录，换机、多环境或 v0.2 服务器常驻时需要改代码。 | 低（不阻塞定稿） | 设计阶段允许环境变量覆盖路径（如 `BRAIN_CREDENTIALS_PATH`），默认值仍为项目根。为 v0.2 常驻调度预留，v0.1 不需实现额外逻辑。 |

- **确认到位、无需修改的部分**（DevOps 视角）：
  - 验收 #9 的凭据纪律写得扎实——异常路径 + **日志/库文件内容扫描断言**，把「不泄露」做成了可自动验证项，而非口号。
  - 验收 #8 的「无确认标记时提交接口零调用」是可断言的硬闸，方向正确。
  - 验收 #5 达标线动态读取，与 `project-context.md` 的硬约束一致。
  - 范围边界明确排除「服务器常驻 / 定时调度」，v0.1 无部署动作 → **部署就绪检查阶段可预期为「已跳过（本迭代无部署，本地运行）」**，届时按规则写明原因。
- **下一步**：H1、M2 由 PM 决定处理或延期；M1 需 Owner 拍板 CI 方案；M3、M4、L1 可结转设计阶段由 Architect 承接。DevOps 本轮不设阻塞，不阻挡定稿。

### R1 · Developer（开发工程师）· 2026-08-15 · 结论：需修改

审查边界：工程成本、分流规则实现可行性、验收标准可验证性、下游实现歧义。不评产品范围取舍（PM 与 Owner 已确认）。

**工程成本评估（结论：范围可控，不构成阻塞）**

- 数据量级无技术障碍：列表 API 自带 settings + code + is 指标 + checks（`brain-platform-facts.md` §4），1 万条按 limit=100 分页约 100+ 次请求即可全量落地，本地库存用单文件嵌入式库足够，无需服务端组件。
- v0.1 可拆为 5 个可独立自测的模块：API 客户端（认证/重试/节流）、库存同步（全量+增量）、分流引擎、提交前检查触发、报告 + 提交流程。单迭代体量合理。
- **实现前提（提请设计阶段确认，非缺陷）**：验收 #2/#4/#5/#8/#9 要求自动化验证，因此平台 API 访问必须收敛到可注入替身的单一客户端层，否则这些用例无法在不触达真实平台的前提下自动化。

**缺陷**

| # | 严重度 | 位置 | 问题 | 建议 |
|---|--------|------|------|------|
| D1 | 高 | 验收 #4、#5，范围边界「规则分流引擎」 | **三分类判定规则全文未定义**。PRD 只写「按 checks 三分类」，未给出「可提交候选 / 改造候选 / 淘汰候选」的判定条件：全 PASS 是否即可提交候选？有 FAIL 时按什么标准区分改造与淘汰（FAIL 项数？value 与 limit 的差距比例？）？`WARNING` / `ERROR` 计不计门槛（UNITS 常见 WARNING、CLUSTER_TEST 样本全 ERROR，后者仅在风险 #4 提到「计为非门槛项」，未进规则正文）？这是产品决策而非工程决策，缺失将导致实现方自行定义规则、验收 #4「分流理由可查询」与 #5「注入不同 limit 断言结果变化」都无法写出确定的断言。 | PM 在正文补 v0.1 初版规则表（哪怕粗粒度：如「无硬门槛 FAIL → 可提交候选；硬门槛 FAIL 且全部 FAIL 项 value/limit 差距 ≤ X → 改造候选；其余 → 淘汰候选」），并明示 WARNING/ERROR 的处理与硬门槛 check 清单来源。 |
| D2 | 中 | 验收 #1 | **验收口径不可判定**。正文要求「本地条数与平台实际总数一致（含突破 count=10000 上限）」，风险 #1 又说该上限可能是平台硬限制、届时改口径——即本条在 R1 状态下无法被判定通过或失败，实现方也无法据此收敛。 | 验收口径改为可判定表述，例如「本地条数 = 分页穷尽可获取的去重条数，且连续两次穷尽结果一致」；「平台实际总数」是否可得作为设计阶段调研结论回填，不作为验收门槛。 |
| D3 | 中 | 验收 #7 与 #1/#3 | **漏斗级「未回测」的数据来源缺失**。#7 要求报告展示「未回测」一级，但 #1 定义的同步范围只有「未提交 + 已提交」，未提交 ≠ 未回测；库存里的记录均来自已回测产物，「未回测」在 v0.1 无数据源（组装端 v0.3 才做）。报告将出现一个恒为 0 或无法计算的层级。 | 明确：v0.1 报告的漏斗层级去掉「未回测」，或注明该级在 v0.1 恒为 0（占位，v0.3 组装端接入后有值）。 |
| D4 | 低 | 验收 #1、范围边界 | 全量同步为百次量级的串行网络请求，中途失败（限流 / 掉线）若无断点续传则整轮重来。不阻塞定稿，提请设计阶段纳入。 | 设计阶段考虑同步进度可恢复；PRD 侧无需改动。 |

**待澄清问题**（不计严重度，答案须由 PM 回填正文，下游实现会话只读正文）

| # | 位置 | 问题 |
|---|------|------|
| Q1 | 验收 #2 | 增量同步的「变更」以什么为判定键？调研事实只记录了 `dateCreated` 可排序，未确认存在 `dateModified` 一类字段。若无变更键，「只拉取新增/变更」只能退化为「只拉取新增」——请明确 v0.1 的增量语义（建议：增量 = 新增；已同步 Alpha 的 checks 变化由 #6 的检查触发流程自身回写，不依赖增量同步）。 |
| Q2 | 验收 #2 与 #6 | 同步与本地派生数据的覆盖关系未定义。#2 要求「重复执行同步，内容不变」，而 #4/#6/#8 会向库存写入分流结果、分流理由、提交前检查结果与人工确认标记。请明确：再次同步时平台字段覆盖、本地派生字段保留——否则「幂等」与「回写」两条验收互相冲突，实现方无法同时满足。 |
| Q3 | 验收 #8 | 「人工确认」的载体与粒度未定义。测试需断言「无确认标记时提交接口零调用」，因此确认必须是可编程实体：是命令行交互确认、独立确认清单文件，还是库存中的确认字段？确认粒度是整批一次确认还是逐条确认？ |
| Q4 | 验收 #6 | 第二梯队 checks 初始为 PENDING、结果需等待平台计算（`brain-platform-facts.md` §2/§6），属异步获取。请明确超时或长期 PENDING 的候选如何处置（保持 PENDING 不进最终清单、不阻塞其余候选？是否需要重试上限），否则 #6 的「获取最终结果」在实现上没有终止条件。 |

**通过项（无异议）**

- 验收 #9（凭据纪律）与 `project-context.md` 硬约束一致，验证方式（异常路径 + 日志/库文件扫描）可自动化。
- 验收 #8 的「人工确认闸」方向正确，与「提交不可逆」硬约束一致（具体载体见 Q3）。
- 范围边界清晰，v0.1 不含 Web / 常驻调度，规避了本迭代最大的成本膨胀源。

**放行条件**：D1 必修（本迭代必须修）；D2、D3 建议本轮一并修正；Q1–Q4 回填正文后，Developer 可在 R2 复核通过。D4 不阻塞定稿。
